# 6. 产品验证矩阵

本文件定义产品规范到自动化验证的映射，用于判断产品行为是否达到面向 PRD、交互和功能的完整验证目标。

`docs/product-spec/` 是产品事实源；本文件只记录验证覆盖状态、验证类型、证据入口和缺口。

## 状态定义

| 状态 | 含义 |
|---|---|
| `missing` | 当前没有持久化自动化验证。 |
| `planned` | 已明确验证方式，但测试用例或脚本尚未落地。 |
| `partial` | 仓库内已有部分自动化验证，但没有覆盖完整范围。 |
| `covered` | 仓库内已有测试或脚本覆盖，并纳入标准门禁。 |
| `manual-evidence` | 有截图、交互记录或人工检查作为短期证据，但没有长期回归测试。 |

## 使用规则

1. 实现或修改产品行为前，先找到对应矩阵行。
2. 如果没有对应行，先补充矩阵行，再实现代码。
3. 行为完成后，将验证状态推进到 `covered`，或写明为什么只能保留为 `manual-evidence`。
4. 产品行为、UI/UX 契约、API 契约、事件契约和验收标准变更时，必须同步检查本矩阵。
5. Playwright 截图、人工观察或本地浏览器检查只能辅助开发和审查，不能长期替代仓库测试。
6. `planned`、`partial` 和 `manual-evidence` 行必须写明目标测试文件或命令、阻塞缺口和关闭条件；不能只写“未来测试”或“后续补充”。
7. 涉及 Meeting Assistant 的行必须引用相关 `TDG-MA-*`，直到 testability gate 关闭并有标准门禁证据。

## Meeting Assistant 范围守卫

1. `OD-MA-008` 已关闭：团队分发、团队支持、团队账号、共享会议库和集中审计不属于当前产品范围；多人使用时各自在本地 Mac 上独立运行。因此本矩阵不新增团队分发相关 `PV-MA-*` 行；未来如重新提出团队协作、共享、分发或集中支持，必须先新增或调整对应 `CAP/AC/PV`。
2. `OD-MA-009` 仍为 `watch`：当前只验证用户主动复制或导出 transcript 后自行整理会议纪要，覆盖入口仍是 `PV-MA-011` 的 no-auto-upload/no-external-API 边界。应用自动调用 GPT、外部 API 或本地 Qwen 进入范围前，必须先走 spec-change，新增 API、安全、数据和验证矩阵覆盖。

## Native app XCUITest 执行策略

`platform/native-app/scripts/test.sh` 默认是快速组件测试入口，覆盖 metadata 检查、Swift Testing 和 XCTest-hosted SwiftUI smoke。真实 `.app` 启动、窗口定位和 `XCUIApplication()` locator 证据由 `platform/native-app/scripts/test-app-bundle.sh` 提供；也可用 `MA_NATIVE_APP_RUN_XCUITEST=1 ./platform/native-app/scripts/test.sh` 在同一次组件测试中启用。下方矩阵行如果引用 `AppBundleLocatorSmokeTests.swift` 或 app-bundle XCUITest，交付证据必须明确记录 `test-app-bundle.sh` 或上述 env-enabled 命令；默认 `test.sh` 的 skip 输出不能单独作为 app-bundle 证据。

## Starter 基线矩阵

| ID | 场景或规则 | 主事实源 | 验证类型 | 目标测试入口 | 当前证据 | 状态 |
|---|---|---|---|---|---|---|
| PV-HARNESS-001 | 事实源分卷、工程分卷、入口脚本、当前模式叙述和产品能力链路一致性 | `docs/product-spec/README.md`、`docs/engineering/README.md` | 文档门禁 | `./scripts/docs-check.sh` | `docs-check` 检查必要分卷、索引、脚本入口、当前 mode 单一声明、project 模式下的 activation 状态漂移，以及 `CAP-MA-*`、`AC-MA-*`、`PV-MA-*` 的双向可追溯链路 | `covered` |
| PV-HARNESS-002 | Agent 工作流可生成 spec sync 和 review report 证据，并能识别 backend、frontend、platform 产品表面 | `05-agent-operating-model.md`、`04-review-and-ci-gates.md` | 工程门禁、自测试 | `./scripts/agent-workflow-check.sh`、`./scripts/review-report.sh --check`、`./scripts/harness-self-test.sh` | workflow gate 调用 spec sync 和 review report check；自测试覆盖 platform source 缺少验证矩阵、platform 实现缺少测试和 review report 推荐验证 | `covered` |
| PV-HARNESS-003 | Docker 优先且禁止宿主机数据库/中间件依赖 | `01-repo-structure.md`、`02-dev-commands.md` | 文档门禁、Compose 配置 | `./scripts/docs-check.sh`、`./scripts/compose-check.sh` | docs-check 扫描宿主机依赖，compose-check 解析本地和测试配置 | `covered` |
| PV-HARNESS-004 | 非 project 模式和空组件不能通过发布门禁 | `02-dev-commands.md`、`04-review-and-ci-gates.md` | Harness 自测试 | `./scripts/harness-self-test.sh` | 单元测试验证 release fail-closed 和未注册组件 | `covered` |
| PV-HARNESS-005 | Agent 使用最小文件、网络、凭据和外部写权限 | `12-agent-security.md` | 策略校验 | `./scripts/project-manifest-check.sh current` | 校验 `harness/agent-policy.json` 固定安全默认值 | `covered` |
| PV-HARNESS-006 | 真实组件必须注册质量、安全、架构和 SBOM 命令，且新增 frontend、backend 或 platform 组件不能绕过 manifest | `01-repo-structure.md`、`10-security-and-supply-chain.md` | 清单门禁、自测试 | `./scripts/project-manifest-check.sh development`、`./scripts/harness-self-test.sh` | project 模式缺少组件或命令即失败；自测试覆盖未注册 `platform/*/component.json` 失败路径 | `covered` |
| PV-HARNESS-007 | 发布候选执行真实 Compose full-stack E2E | `03-test-strategy.md`、`11-production-readiness.md` | 运行门禁、自测试 | `./scripts/test-e2e-full-stack.sh`、`./scripts/harness-self-test.sh` | project 清单驱动 pre-start、本地 smoke image 准备、`compose up --wait`、seed、测试和清理；本地入口默认使用 `meeting-assistant-smoke:local`，在有界窗口内反复扫描 target 和允许的 cached base images，且不自动 pull，release compose 默认值保持 digest-pinned；自测试验证 full-stack runner 执行 pre-start/config/up/seed/test/cleanup 顺序 | `covered` |
| PV-HARNESS-008 | 交付审查引用实际命令、退出码、commit、worktree fingerprint、日志和阶段/发布验证状态 | `04-review-and-ci-gates.md` | 证据门禁 | `./scripts/check.sh`、`./scripts/product-validation-check.py current-phase`、`./scripts/phase-preflight.sh`、`./scripts/review-report.sh --require-evidence` | `.harness/evidence/` 记录并验证当前工作区命令结果；`product-validation-check.py current-phase` 允许 documented `partial/planned` 行继续阶段集成，`release` scope 只输出 concise blockers 且要求全部 `PV-*` 为 `covered` | `covered` |
| PV-HARNESS-009 | 不完整初始意向只能进入 adoption 工作区，不能直接变成产品事实或业务实现 | `09-starter-adoption-guide.md`、`14-greenfield-project-start.md` | Adoption 门禁、自测试 | `./scripts/adoption-check.sh`、`./scripts/harness-self-test.sh` | adoption check 阻断空 initial request、template marker、未完成 readiness 和无确认 activation | `covered` |
| PV-HARNESS-010 | Project activation 必须显式确认且 fail-closed；placeholder、blocking open decision、缺少 manifest 或 PV 行都会阻断 | `09-starter-adoption-guide.md`、`04-review-and-ci-gates.md` | Adoption activation 门禁 | `./scripts/adoption-check.sh --activation`、`./scripts/activate-project.sh` | activate-project 先写入确认、运行 activation check，失败回滚到 adoption | `covered` |

## Meeting Assistant 产品验证矩阵

| ID | 场景或规则 | 主事实源 | 验证类型 | 目标测试入口 | 当前证据 | 状态 |
|---|---|---|---|---|---|---|
| PV-MA-001 | `CAP-MA-001` 录制前权限和环境预检 fail closed | `docs/product-spec/01-product-scope.md`, `docs/product-spec/04-user-journeys-and-ui.md`, `docs/product-spec/06-api-contracts.md`, `docs/product-spec/13-security-and-compliance.md` | Native app 状态测试、本地命令契约测试、权限状态模拟、受控 macOS permission preflight 测试 | `platform/processing-cli/scripts/test.sh`; `platform/native-app/scripts/test.sh`; `platform/native-app/tests/MeetingAssistantNativeTests/PermissionDependencyStatusViewModelTests.swift`; `platform/native-app/tests/MeetingAssistantNativeTests/NativeRecordingCommandClientTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeUITests/NativeControlPlaneSmokeTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeAppUITests/AppBundleLocatorSmokeTests.swift`; `platform/native-app/MeetingAssistantNative.xcodeproj`; `platform/native-app/scripts/native-capture-smoke.sh`; `platform/native-app/scripts/test-app-bundle.sh` | 当前证据：`processing-cli` 已能报告平台、workspace、依赖、CLI best-effort permission unknown/denied 状态和缺失必需依赖 `ok=false`。VS-MA-12 已在 `native-app` 增加 11 个 Swift Testing 用例：解码 `check_dependencies` JSON、缺失必需依赖阻断 processing、`permission.denied`/`unknown` 对录制 fail closed、ready 状态、runner failure、真实 process bridge 参数/结构化非零 JSON、`ok=false` 泛化阻断文案和稳定 accessibility identifier；P2-D 保留 XCTest-hosted SwiftUI 等价 smoke，覆盖 `permission.screen_recording=denied`、`media_tool.ffmpeg=missing`、readiness blocked 和 `ma.recording.startButton` blocked 状态，并通过生产 SwiftUI source contract 断言 `ma.permissionDependency.*` / 相关 row locator 与关键可见文案仍由视图定义使用。`NativeRecordingCommandClientTests.macOSScreenRecordingPreflightDeniedFailsClosedWithoutStartingAdapter` 通过 `MacOSNativeCapturePermissionChecker` 和注入的 `CoreGraphicsScreenRecordingPermissionProbe(preflight: { false })` 覆盖受控 macOS Screen Recording denied 路径：返回 `permission_denied`，不启动 capture adapter，不写 session；同组测试还覆盖 requested microphone unknown 时 fail closed，未请求 microphone 时不阻断 screen-only capture。新增持久 `MeetingAssistantNative.xcodeproj`、最小 app shell 和 app-bundle XCUITest，`AppBundleLocatorSmokeTests` 使用 `XCUIApplication()` 启动 `.app`，默认 blocked fixture 查询 `ma.permissionDependency.*`、`ma.permission.permission.screen_recording.status`、`ma.dependency.media_tool.ffmpeg.status`、`ma.recording.heading/readinessStatus/status/startButton/stopButton` 等 locator，并验证 start button 在 blocked readiness 下禁用；opt-in real capture app-bundle smoke 在缺少本机 TCC 权限时保留失败退出和 `permission_denied` UI/error 证据，授予权限后可证明 screen-only capture，但该真实录制成功仍归 `PV-MA-002/003` 继续 partial。`platform/native-app/scripts/test.sh` 标准门禁包含上述 Swift Testing 和 hosted smoke；`build.sh`、`architecture.sh` 和 `security.sh` 覆盖 app/project/ui test 文件。关闭条件已满足：真实/受控 macOS 权限负向路径已进入组件标准门禁，且 release 成功录制本身不属于 `PV-MA-001` 的权限 fail-closed 关闭条件 | `covered` |
| PV-MA-002 | `CAP-MA-002` 原生录制启动、持续状态和停止保存 | `docs/product-spec/04-user-journeys-and-ui.md`, `docs/product-spec/06-api-contracts.md`, `docs/product-spec/08-implementation-guidance.md`, `docs/product-spec/09-acceptance-criteria.md` | Swift Testing、XCUITest、受控 native recording smoke、显式 opt-in 真实 native capture smoke | `platform/native-app/scripts/test.sh`; `platform/native-app/tests/MeetingAssistantNativeTests/RecordingControlViewModelTests.swift`; `platform/native-app/tests/MeetingAssistantNativeTests/RecordingStateMachineTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeUITests/NativeControlPlaneSmokeTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeAppUITests/AppBundleLocatorSmokeTests.swift`; `platform/native-app/scripts/native-capture-smoke.sh`; `platform/e2e/native-capture-artifact-smoke.sh`; `platform/e2e/release-native-capture-artifact-smoke.sh`; `platform/native-app/MeetingAssistantNative.xcodeproj` | 当前证据：deterministic fake recording client、SwiftUI recording control、Swift Testing、hosted smoke 和 app-bundle XCUITest 已覆盖 recording state/UI/locator；Apple ScreenCaptureKit adapter、显式 opt-in native capture smoke 和显式 opt-in app-bundle real capture smoke 已证明本机 screen-only 真实录制、`screen_video` 非空、checksum、permission fail-closed 和 designed shell 主操作触发路径；本轮新增 `platform/e2e/native-capture-artifact-smoke.sh` 证明真实 native capture 后的 recorded session 可被 processing workspace contract 读取；非 XCTest app runtime 的 production/default recording client 已由 `MeetingAssistantNativeApp` source-contract 和 native architecture guard 证明默认选择 `NativeRecordingCommandClient` + `MacOSNativeCapturePermissionChecker` + `AppleScreenCaptureKitNativeCaptureAdapter`，Debug/XCTest fake/controlled hook 保持隔离；本轮新增 release-scope wrapper `platform/e2e/release-native-capture-artifact-smoke.sh`，让 `scripts/release-preflight.sh` 在 release PV 闸门之后具备标准 native capture artifact gate。阻塞缺口：release-scope native capture gate 已补入并在本机通过，但跨机器 TCC/display 可重复性、独立系统音频/麦克风音频产物、真实 capture 到后续 transcript/export/delete 的 release-scope 同链路 smoke 和 release bundle 仍未关闭。关闭条件：release-scope native recording gate 在受控 TCC 环境可重复通过，真实录制成功和权限/显示失败均有标准证据，且真实 native-to-processing release-scope 链路闭合后才能考虑 `covered` | `partial` |
| PV-MA-003 | `CAP-MA-003` 会话和录制产物登记，包含降级原因 | `docs/product-spec/02-domain-model.md`, `docs/product-spec/05-business-rules-and-calculations.md`, `docs/product-spec/07-data-and-events.md` | 文件契约测试、录制 artifact registry 测试、受控 E2E、显式 opt-in 真实 native capture artifact E2E | `platform/processing-cli/scripts/test.sh`; `platform/native-app/scripts/test.sh`; `platform/native-app/scripts/native-capture-smoke.sh`; `platform/e2e/native-capture-artifact-smoke.sh`; `platform/e2e/release-native-capture-artifact-smoke.sh` | 当前证据：`processing-cli` workspace contract 已覆盖 `session.json`、artifact registry、checksum、路径边界、单会话 lock 和损坏 metadata fail-closed；native stop artifact registration 已覆盖四类 artifact、checksum、降级原因、path-conflict retry、restart recovery 和 adapter path 防御；本轮真实 native capture smoke 已验证 `screen_video` available、三类音频 missing/degraded 必须带 `degradation_reason`，且新增 E2E 证明 processing workspace contract 可读取真实 native stop 结果并在无可用音频时 fail closed、不登记派生产物；本轮 release-scope wrapper 让同一结构化 report 可声明 `release_gate=release-scope-native-capture` 并进入 release-preflight 后续步骤。阻塞缺口：release-scope stop artifact gate 已补入并在本机通过，但独立系统音频/麦克风音频真实产物策略未关闭，production/default 真实 capture 到 processing、transcript/export/delete 和 release bundle 的同链路成功证据仍未证明。关闭条件：四类 artifact 在 release-scope gate 中可重复验证，音频不可用时有明确产品化降级证据，且至少一条真实 native 录制产物链能进入后续 processing 成功路径后才能考虑 `covered` | `partial` |
| PV-MA-004 | `CAP-MA-004` 导入已有媒体作为回退或测试路径，覆盖格式白名单、artifact type 映射、源文件保护和 `invalid_input` 边界 | `docs/product-spec/04-user-journeys-and-ui.md`, `docs/product-spec/06-api-contracts.md`, `docs/product-spec/07-data-and-events.md` | 本地命令契约测试、文件契约测试、fixture media smoke | `platform/processing-cli/scripts/test.sh`; `platform/processing-cli/tests/test_cli_contract.py`; `platform/e2e/smoke-test.sh` | 当前证据：`processing-cli` 已实现 `import_media`，组件测试覆盖 `.wav`/`.mp4` artifact 映射、CLI JSON 成功响应、源文件不覆盖、会话内 artifact 副本、无派生产物、源路径 symlink 复制解析后 regular file 且不登记 link，以及 unsupported suffix、缺失路径、目录输入的 `invalid_input`；`test_cli_contract.py` 覆盖缺失 `--path`、unknown argument、非冻结 `--format` 参数拒绝、单 JSON failure shape 和 exit code 2。P2-C local smoke `platform/e2e/smoke-test.sh` 已纳入 `platform/processing-cli/scripts/test.sh`，通过临时 workspace 和确定性 WAV fixture 断言导入源 checksum 不变、会话内 `mixed_audio` 副本和 `session.json` 初始 artifact 状态；同一 smoke 新增真实媒体 decode import 检查，使用本机 `ffmpeg`/`ffprobe` 生成并解析小型 `.wav`/`.mp4` fixture，分别导入为 `mixed_audio`/`screen_video`，断言源文件和导入 artifact 均可解析、checksum 一致、format/type/schema 正确、不产生派生产物，并断言 unsupported suffix 返回 `invalid_input` 且不新增 session。`platform/e2e/full-stack-smoke.sh` 复用该 smoke。当前 AC/CMD 只定义 `import_media` 命令面作为回退或测试路径，native file picker/import UI 若进入 release 范围应作为后续 UI 扩展另行补证。关闭条件已满足 | `covered` |
| PV-MA-005 | `CAP-MA-005` 本地依赖检查稳定 JSON 响应，缺失必需依赖 `ok=false`，不自动下载 | `docs/product-spec/06-api-contracts.md`, `docs/product-spec/08-implementation-guidance.md`, `docs/product-spec/13-security-and-compliance.md`, `docs/engineering/10-security-and-supply-chain.md` | 本地命令契约测试、fake 环境测试、安全扫描、Native app 状态测试、release provider smoke | `platform/processing-cli/scripts/test.sh`; `platform/processing-cli/tests/test_cli_contract.py`; `platform/processing-cli/scripts/security.sh`; `platform/processing-cli/scripts/release-provider-smoke.sh`; `platform/native-app/scripts/test.sh`; `platform/native-app/tests/MeetingAssistantNativeTests/PermissionDependencyStatusViewModelTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeUITests/NativeControlPlaneSmokeTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeAppUITests/AppBundleLocatorSmokeTests.swift`; `platform/native-app/MeetingAssistantNative.xcodeproj`; `./scripts/test.sh`; `./scripts/lint.sh` | 当前证据：`processing-cli` 已实现 `check_dependencies` JSON 响应、平台/架构/Swift/FFmpeg/`whisper.cpp` runtime/multilingual model/transcription hardware preflight/speaker fallback/workspace/权限状态/允许来源/不自动下载 checks，并在缺失 runtime/model 时提示 `~/.local` 共享资产推荐路径；组件测试覆盖成功、缺失必需依赖 `ok=false`、English-only `.en` 模型拒绝、large-v3/turbo 模型硬件 preflight 的 supported/constrained/unsupported 状态、CLI `dependency_missing` exact exit code，以及 CLI 缺必填、unknown flag、非法 enum 的结构化 `invalid_input` JSON；`test_cli_contract.py` 覆盖 `check_dependencies` unknown argument、非法 `--format` enum、成功单 JSON shape，并验证只有 `check_dependencies` 接受冻结的 `--format json|pretty`。`platform/processing-cli/scripts/security.sh` 扫描 `src`、`tests`、组件 `scripts`、`component.json`、`README.md` 和 `platform/e2e`，继续阻断 curl/wget、install 命令、secret marker 和自动下载路径。VS-MA-12 native Swift Testing 覆盖 `check_dependencies` contract decode、缺失必需依赖 view model 阻断、ready 状态、runner failure、真实 process bridge 参数/结构化非零 JSON、`ok=false` 泛化阻断文案和 dependency status accessibility identifier；P2-D 保留 XCTest-hosted SwiftUI 等价 smoke，将缺失依赖 `media_tool.ffmpeg=missing`、权限 denied、`ma.permissionDependency.*` / `ma.recording.*` source-contract locator 和关键可见文案检查纳入 `platform/native-app/scripts/test.sh`；app-bundle XCUITest 使用 deterministic blocked/ready fixtures 查询 `ma.dependency.media_tool.ffmpeg.status`、`ma.permissionDependency.summary` 和 recording readiness locator，证明 dependency-state UI 在真实 `.app` 中可启动和可查询，且不调用真实 helper、processing bridge、下载或外部 API。2026-07-04 release provider smoke 已在本机 `~/.local` 共享资产边界下通过：`check_dependencies --format json` 返回单 JSON 且 `ok=true`，断言 `transcription.runtime=available`、`transcription.model=available`、`transcription.model.multilingual=reported`、`transcription.hardware=supported`、`dependency_downloads.automatic=not_attempted`；runtime `~/.local/opt/whisper.cpp/v1.9.1/bin/whisper-cli` 可执行，模型 `~/.local/share/ai-models/whisper.cpp/large-v3-turbo/ggml-large-v3-turbo-q5_0.bin` 通过 sha256 `394221709cd5ad1f40c46e6031ca61bce88931e6e088c188294c6d5a55ffa7e2`、license sidecar 和 provenance sidecar 校验，默认 mixed-language fixture `~/.local/share/ai-fixtures/asr/zh-en-tech/mixed-zh-en-tech.wav` 位于允许根目录，并强制通过 `whisper.cpp` mixed-language smoke；脚本同时证明 no-auto-download/no-auto-upload 边界未改变。该侧车证据是当前本机 release-scope provider 证据，其他机器需各自准备等价 `~/.local` runtime/model/fixture/sidecar。关闭条件已满足 | `covered` |
| PV-MA-006 | `CAP-MA-006` 标准化处理音频和输入选择，不覆盖原始音频 | `docs/product-spec/05-business-rules-and-calculations.md`, `docs/product-spec/07-data-and-events.md`, `docs/product-spec/09-acceptance-criteria.md` | 文件契约测试、样例媒体处理测试、source selection 单元测试 | `platform/processing-cli/scripts/test.sh`; `platform/processing-cli/scripts/architecture.sh`; `platform/processing-cli/tests/test_audio_processing.py`; `platform/processing-cli/tests/test_workspace_contract.py`; `platform/e2e/smoke-test.sh`; `platform/e2e/full-stack-smoke.sh`; `platform/native-app/scripts/test.sh`; `platform/native-app/tests/MeetingAssistantNativeTests/ProcessingStateViewModelTests.swift`; 后续 production-scope native 处理入口测试 | 当前证据：`processing-cli` 已实现 fixture-compatible normalized audio stage，覆盖默认选择 `mixed_audio`、显式 fallback 音频、`artifact_missing`、WAV/PCM 登记、重复执行复用、换源冲突、adapter 失败清理、登记失败和校验失败回滚，且架构脚本断言没有公开 `normalize_audio` 命令。Line B 强化 provider managed-output path 边界，`prepare_managed_output_path` 和 audio processing 测试覆盖 `normalized_audio.wav` 目标、`.normalized_audio.wav.tmp` 临时文件和 `logs/processing.log` 写入前的 symlink、broken symlink、hardlink、非 regular file、symlink parent、异常 parent 和越界路径 fail-closed；失败沿用 `path_conflict`，不登记 `normalized_audio`，也不写入外部目标。P2-C local smoke 通过 `generate_transcript` 间接触发 normalized audio/source selection，断言 `normalized_audio.wav`、transcript 的 `source_artifact_id` 指向 normalized artifact，且原始 `mixed_audio` 和导入源 checksum 不变。Integration bridge smoke `platform/e2e/native-transcript-bridge-smoke.sh` 通过 manifest wrapper 复用公开 CLI 生成临时 workspace，并在 native 消费前断言 `normalized_audio`、`transcript_text` 和 `speaker_labels` artifact file/checksum 仍在同一 session 边界内。2026-07-04 新增 `ProcessingStateViewModelTests.processRunnerWithRealProcessingCLIWritesWorkspaceArtifactsLoadableByTranscriptReview`，先用真实 `processing-cli import_media` 登记 WAV，再通过 `ProcessingCommandProcessRunner` 调真实 CLI 执行 `generate_transcript`/`generate_speaker_labels`，断言 `mixed_audio`、`normalized_audio`、`transcript_text`、`speaker_labels` artifact 顺序落盘且导入源 checksum 不变，并由 native loader 读取同一 workspace。阻塞缺口：VS-MA-16 的 opt-in app-bundle UI -> processing-cli provider 触发已通过，VS-MA-20 也已有 opt-in controlled MVP full-stack app-bundle smoke；仍未关闭的是 release-scope full-stack 中真实 native capture 产物、processing、transcript review/export/delete 和 release bundle 的同链路标准门禁。关闭条件：VS-MA-20/release-scope smoke 串起 designed shell + native recording + processing + transcript/export/delete 并纳入标准门禁 | `partial` |
| PV-MA-007 | `CAP-MA-007` 生成按时间排序的 transcript segments | `docs/product-spec/02-domain-model.md`, `docs/product-spec/05-business-rules-and-calculations.md`, `docs/product-spec/06-api-contracts.md` | 本地命令契约测试、adapter fake 测试、样例 transcript 测试、条件真实 runtime smoke | `platform/processing-cli/scripts/test.sh`; `platform/processing-cli/tests/test_cli_contract.py`; `platform/processing-cli/tests/test_transcript_processing.py`; `platform/processing-cli/scripts/smoke-whisper-cpp.sh`; `platform/e2e/smoke-test.sh`; `platform/e2e/full-stack-smoke.sh`; `platform/native-app/scripts/test.sh`; `platform/native-app/tests/MeetingAssistantNativeTests/ProcessingStateViewModelTests.swift`; 后续 production-scope native 处理入口测试 | 当前证据：`processing-cli` 已实现 `generate_transcript` fake adapter 和最小 `whisper.cpp` adapter；组件测试覆盖 normalized/default audio、显式 `system_audio`、segments 排序、`start_ms < end_ms`、`artifact_missing`、`processing_failed`、adapter 异常不覆盖原始音频、非法 runtime、runtime/model 缺失、English-only `.en` 模型拒绝、fake `whisper.cpp` CLI JSON 成功解析、runtime 非零退出、JSON/segments 无效、真实 whisper.cpp token 级 JSON 非 UTF-8 字节容错、显式 runtime 不复用缺少 runtime metadata 的旧 transcript、复用/冲突/checksum drift/回滚，以及强制 smoke 拒绝非 `large-v3`/`large-v3-turbo` 名称模型；Line B 增加 `transcript.json` managed-output 写入前检查，覆盖 symlink、broken symlink、hardlink、非 regular file 和越界路径 fail-closed，失败时返回既有 `path_conflict`，不覆盖外部目标且不登记 `transcript_text`。`test_cli_contract.py` 覆盖缺失 `--session-id`、unknown argument、非冻结 `--format` 参数拒绝、unsupported runtime `invalid_input`、单 JSON failure shape 和 exit code 2。`smoke-whisper-cpp.sh` 已由 `platform/processing-cli/scripts/test.sh` 调用；在本机共享 runtime `~/.local/bin/whisper-cli` -> `~/.local/opt/whisper.cpp/v1.9.1/bin/whisper-cli`，runtime source `ggml-org/whisper.cpp` tag `v1.9.1` commit `f049fff95a089aa9969deb009cdd4892b3e74916`，模型 `~/.local/share/ai-models/whisper.cpp/large-v3-turbo/ggml-large-v3-turbo-q5_0.bin` sha256 `394221709cd5ad1f40c46e6031ca61bce88931e6e088c188294c6d5a55ffa7e2`，以及默认 fixture `~/.local/share/ai-fixtures/asr/zh-en-tech/mixed-zh-en-tech.wav` 配置下，强制真实 smoke 已通过，并断言 transcript 含中文字符以及 `HTTP`、`LLM`、`clean architecture`、`EDA`。P2-C local smoke 通过 CLI fake adapter 断言 transcript JSON、`transcript_id`、`artifact_id`、`segment_count`、segments 排序和时间范围。Integration bridge smoke 再用 `TranscriptReviewWorkspaceLoader`/`TranscriptReviewViewModel` 读取 processing-cli 生成的 `transcript.json`，断言 transcript text 和 segment count 投射到 native read model。2026-07-04 native test 通过 `ProcessingCommandProcessRunner` 调真实 CLI 写出 transcript，并断言 `TranscriptReviewWorkspaceLoader` 读回同一 transcript id、session id、`segment-0001` 和 `Fake transcript generated from local audio.` 文本；缺少配置时只能报告 not run，P2-C fake smoke、bridge smoke 和本 native fake-adapter CLI 证据仍不能单独替代真实 runtime 质量。阻塞缺口：VS-MA-16 的 opt-in app-bundle UI -> processing-cli provider 触发已通过，VS-MA-20 也已有 opt-in controlled MVP full-stack app-bundle smoke；仍未关闭的是 release-scope full-stack 中真实 native capture 产物、processing、transcript review/export/delete、release bundle 和目标环境真实 runtime 的同链路标准门禁。关闭条件：VS-MA-20/release-scope smoke 串起 designed shell + native recording + processing + transcript/export/delete，并确认真实 runtime smoke 在当前目标环境稳定运行后，才能将 `PV-MA-007` 从 `partial` 推进到 `covered` | `partial` |
| PV-MA-008 | `CAP-MA-008` best-effort 匿名 speaker labels 和 transcript-only fallback | `docs/product-spec/02-domain-model.md`, `docs/product-spec/05-business-rules-and-calculations.md`, `docs/product-spec/13-security-and-compliance.md` | 单元测试、样例 transcript 测试、fallback 契约测试 | `platform/processing-cli/scripts/test.sh`; `platform/processing-cli/tests/test_cli_contract.py`; `platform/processing-cli/tests/test_speaker_labeling.py`; `platform/e2e/smoke-test.sh`; `platform/e2e/full-stack-smoke.sh`; `platform/native-app/scripts/test.sh`; `platform/native-app/tests/MeetingAssistantNativeTests/ProcessingStateViewModelTests.swift`; 后续 production-scope native processing state tests | 当前证据：`processing-cli` 已实现 `generate_speaker_labels` transcript-only fallback 和可注入 adapter 边界；组件测试覆盖 `speaker_labels.json` schema、`label_status=transcript_only`、`degradation_reason`、降级 artifact metadata、fake adapter `SPEAKER_01` 匿名格式、`is_verified_identity=false`、segment mapping 只能引用当前 transcript、既有 `speaker_labels.json` 复用时重新校验当前 transcript segment、adapter 身份声明或越界 mapping 降级、缺失 transcript `artifact_missing`、fallback disabled `processing_failed`、重复执行复用、不修改 transcript text、Python 3.9 标准 `python3` 导入回归，以及 CLI success/missing required/illegal enum/unknown argument exit code；Line B 增加 `speaker_labels.json` managed-output 写入前检查，覆盖 symlink、broken symlink、hardlink、非 regular file 和越界路径 fail-closed，失败时返回既有 `path_conflict`，不覆盖外部目标且不登记 `speaker_labels`。`test_cli_contract.py` 覆盖缺失 `--transcript-id`、非法 fallback enum、unknown argument、非冻结 `--format` 参数拒绝、单 JSON failure shape 和 exit code 2。P2-C local smoke 串联 transcript 后通过 CLI 生成 transcript-only fallback，断言 `label_status=transcript_only`、`speaker_labels` artifact schema、空 labels/segment_mapping、显式 `degradation_reason` 和原始媒体 checksum 不变；组件测试继续覆盖既有 `speaker_labels.json` 复用时的当前 transcript segment 再校验。Integration bridge smoke 断言 processing-cli 生成的 transcript-only `speaker_labels.json` 与 artifact metadata 可经 native view model 显示为降级原因。2026-07-04 native test 通过 `ProcessingCommandProcessRunner` 调真实 CLI 写出 transcript-only speaker fallback，断言 native processing state 进入 degraded、`label_status=transcript_only`，并由 `TranscriptReviewWorkspaceLoader` 读回空 labels/segment mapping 与降级原因。阻塞缺口：VS-MA-16 的 opt-in app-bundle processing state/provider 触发已通过，VS-MA-20 也已有 opt-in controlled MVP full-stack app-bundle smoke；仍未关闭的是 release-scope full-stack 中真实 native capture、processing、transcript review/export/delete、release bundle 和可选真实 diarization adapter 的同链路证据；真实 diarization runtime/质量不属于 P2-C 默认承诺，若选择具体 runtime 需另走 spec-change。关闭条件：release-scope full-stack smoke 和后续真实 adapter 进入范围时补对应 UI/runtime 证据后再考虑 `covered` | `partial` |
| PV-MA-009 | `CAP-MA-009` 文件化流水线重试和原始媒体保护 | `docs/product-spec/02-domain-model.md`, `docs/product-spec/05-business-rules-and-calculations.md`, `docs/product-spec/07-data-and-events.md` | 文件契约测试、处理重试测试、并发/路径冲突测试 | `platform/processing-cli/scripts/test.sh`; `platform/processing-cli/tests/test_audio_processing.py`; `platform/processing-cli/tests/test_transcript_processing.py`; `platform/processing-cli/tests/test_speaker_labeling.py`; `platform/processing-cli/tests/test_workspace_contract.py`; `platform/e2e/smoke-test.sh`; `platform/e2e/full-stack-smoke.sh`; 后续并发和 native-processing bridge tests | 当前证据：workspace contract 覆盖原始媒体 append-only、checksum 漂移、路径越界、artifact symlink/hardlink fail-closed、managed output path symlink/broken symlink/hardlink/non-regular/symlink-parent fail-closed、损坏 `session.json` 稳定错误和 lock 冲突；Line B 将 provider 侧派生产物写入前检查复用于 `normalized_audio.wav`、`.normalized_audio.wav.tmp`、`transcript.json`、`speaker_labels.json` 和 `logs/processing.log`，覆盖外部目标未被写入、失败时不登记 artifact、既有 `path_conflict` 语义不漂移。normalized audio 和 transcript fake adapter 测试覆盖重复执行、失败日志、temp 清理、回滚和不覆盖原始媒体；speaker fallback 测试覆盖重复执行复用、既有 speaker label artifact 再校验和 transcript 不变；export 测试覆盖导出不修改 transcript 或原始音频、目标路径冲突不覆盖、metadata 记录失败时清理已写 target；delete 测试覆盖确认删除摘要、workspace 外导出保留、session root symlink escape、session root symlink 指向 workspace sibling session fail-closed、`sessions/` 目录 symlink fail-closed、session 内 symlink 只删除链接、locked session `path_conflict`、损坏 session metadata 不删除、permission failure 摘要和 workspace 级删除事件记录。P2-C local smoke 将 import、normalized audio、transcript、speaker fallback、export 和 delete 串成同一临时 workspace 流水线，断言原始媒体 checksum 不变、每步 JSON/exit code/artifact/`session.json` 状态和删除事件；命令 schema、session lock、损坏 `session.json`、metadata 写失败清理、speaker label 复用再校验和 symlink/hardlink fail-closed 由同一组件 test 入口保留。Integration bridge smoke 保留同一临时 workspace 到 native 只读加载结束，断言 source fixture checksum 不变，并由 native loader 的 workspace tests 覆盖 artifact path escape、symlink artifact 和 checksum drift fail-closed。阻塞缺口：`TDG-MA-005` native capture 发布范围产物链路、真实 capture 到 transcript/export/delete 的同链路 release-scope smoke，以及真实录制路径的更完整并发重试仍未进入 release-scope 标准门禁；VS-MA-20 opt-in controlled MVP full-stack smoke 已补同一 workspace/session 阶段证据，VS-MA-21 processing failure retry、native stop path-conflict retry 和 app/client restart recovery 已在下方 2026-07-04 证据回填记录。关闭条件：发布范围内所有派生产物重试与原始媒体保护路径纳入标准门禁 | `partial` |
| PV-MA-010 | `CAP-MA-010` transcript 回查状态显示时间戳、文本、匿名 labels 或降级原因 | `docs/product-spec/04-user-journeys-and-ui.md`, `docs/product-spec/12-ui-ux-design.md` | Native app 状态测试、XCUITest、可访问性 locator 检查 | `platform/native-app/scripts/test.sh`; `platform/native-app/tests/MeetingAssistantNativeTests/TranscriptReviewViewModelTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeUITests/NativeControlPlaneSmokeTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeAppUITests/AppBundleLocatorSmokeTests.swift`; `platform/native-app/MeetingAssistantNative.xcodeproj`; `platform/e2e/native-transcript-bridge-smoke.sh`; `platform/e2e/full-stack-smoke.sh`; 后续 native processing bridge/export-copy UI tests | 当前证据：Line A 在 `native-app` 增加只读 transcript review read model、view model 和 SwiftUI surface，基于 deterministic transcript fixture 覆盖 timestamped segments、按时间排序、transcript text、匿名且非 verified speaker labels、speaker label session mismatch filtering、transcript-only degradation、missing/empty states、稳定 `ma.transcript.*` accessibility locators，并在 hosted smoke 与 app-bundle XCUITest 中查询 transcript heading、summary、segment row、timestamp、text、speaker label、degradation、empty/missing 状态；speaker label 可见时不同时显示 degradation，匿名文案不声称真实身份；app-bundle XCUITest helper 会在每个 launch 前终止旧实例、等待 app/window 前台可用，在有内置屏幕的本机通过 `MA_NATIVE_APP_TEST_DISPLAY=built-in` 将窗口定位到内置屏并断言窗口中心落在内置屏 frame 内，并在点击前重新查询按钮和等待 hittable，避免连续 root gate 中复用失效窗口或按钮引用。Integration bridge 增加 `TranscriptReviewWorkspaceLoader`，只读加载 workspace `sessions/<session_id>/session.json` 中的 `transcript_text` 和可选 `speaker_labels` artifact，拒绝 session id traversal、`session.json` symlink/hardlink/non-regular file、artifact path escape、symlink/hardlink artifact、非 regular file、checksum drift 和 speaker label session mismatch；native Swift Testing 覆盖真实 workspace loader 的成功、缺失、mismatch 和 fail-closed 路径，app-bundle XCUITest 覆盖 `MA_NATIVE_TRANSCRIPT_WORKSPACE` + `MA_NATIVE_TRANSCRIPT_SESSION_ID` 临时 workspace fixture 的 `ma.transcript.*` locator。`platform/e2e/native-transcript-bridge-smoke.sh` 通过公开 processing-cli 生成临时 workspace/session artifacts 后，用 native loader/view model 读取同一 workspace，断言 processing transcript text、segment count 和 transcript-only degradation 可投射到 native read model；manifest full-stack 入口 `platform/e2e/full-stack-smoke.sh` 已包含该 bridge smoke。阻塞缺口：本轮证明 deterministic processing-cli artifacts 可被 native read model 消费，app-bundle 可从临时 workspace fixture 展示 transcript，VS-MA-16 real-processing app-bundle smoke 已证明 native UI 可触发 processing provider 并进入 transcript-only 完成状态；2026-07-05 real action app-bundle smoke 进一步证明真实 processing artifact 可进入 transcript review，同一 smoke 已覆盖 copy/export/delete 主操作入口；2026-07-05 VS-MA-20 opt-in controlled MVP full-stack app-bundle smoke 已补同一 workspace/session 的阶段链路，但仍不证明真实 native capture、真实 runtime/model 质量、真实 speaker generation、真实用户 Save Panel、release bundle 或 release-scope full-stack。关闭条件：真实 native capture、native-processing bridge 与 transcript review/export/delete 入口按 release-scope 范围纳入标准门禁后，再评估推进 `covered` | `partial` |
| PV-MA-011 | `CAP-MA-011` transcript 复制或导出由用户主动触发，应用不自动上传外部 GPT、本地 Qwen 或其他模型 API | `docs/product-spec/06-api-contracts.md`, `docs/product-spec/07-data-and-events.md`, `docs/product-spec/13-security-and-compliance.md` | 本地命令契约测试、导出文件测试、安全边界测试、UI 状态测试 | `platform/processing-cli/scripts/test.sh`; `platform/processing-cli/tests/test_cli_contract.py`; `platform/processing-cli/tests/test_export_transcript.py`; `platform/processing-cli/scripts/security.sh`; `platform/native-app/scripts/test.sh`; `platform/native-app/tests/MeetingAssistantNativeTests/TranscriptReviewActionsViewModelTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeUITests/NativeControlPlaneSmokeTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeAppUITests/AppBundleLocatorSmokeTests.swift`; `platform/e2e/smoke-test.sh`; 后续 native-to-processing command bridge 和 release bundle checks | 当前证据：`processing-cli` 已实现 `export_transcript`，组件测试覆盖 `plain_text`/`markdown`/`json` content 或 target file 输出、`ExportPackage` 摘要写入 `session.json exports[]`、缺失 transcript `artifact_missing`、非法 export type、目标路径冲突不覆盖、metadata 记录失败时清理已写 target、CLI JSON 成功和 illegal enum/unknown argument exit code，并断言导出不修改 transcript 或原始音频；Line B 进一步强化 export provider 契约，覆盖 `artifact_missing` exit 3、`path_conflict` exit 3、existing target 不覆盖、transcript symlink/checksum drift fail-closed、成功路径无外部网络或子进程副作用，以及缺失 `--session-id` required-arg matrix。`test_cli_contract.py` 覆盖缺失 `--export-type`、非法 export enum、unknown argument、非冻结 `--format` 参数拒绝、缺失 target parent 不写文件、单 JSON failure shape 和 exit code 2。P2-C local smoke 使用显式 workspace 外 `target_path` 导出 Markdown，断言导出摘要写入 `session.json exports[]`，且 delete 后外部导出保留。Line A 增加 native `TranscriptReviewActionsView`、`TranscriptReviewActionsViewModel` 和 `TranscriptActionCommandClient` 冻结响应 consumer；Swift Testing 覆盖 copy/export 成功、失败、缺失字段、invalid response、clipboard/destination 行为，hosted smoke 与 app-bundle XCUITest 覆盖 `ma.transcriptAction.copyButton`、`ma.transcriptAction.exportButton`、`ma.transcriptAction.status`、`ma.transcriptAction.success`、`ma.transcriptAction.error` 等用户主动触发 locators。组件 security gate 扩展扫描组件脚本和 `platform/e2e`，继续禁止网络下载、安装命令和 secret marker；P2-C smoke 与 native fake command client 不调用外部 API，不自动上传 transcript、音频或视频。本分支新增 Debug/XCTest-only `MA_NATIVE_TRANSCRIPT_ACTION_CLIENT=process` app-bundle smoke，通过 native-owned fixture 驱动 `TranscriptActionProcessRunner`，断言用户触发 copy/export invocation、真实 Markdown 导出文件写入和安全 bridge failure 展示。本轮新增 production-injected `TranscriptActionOSClients.swift`，生产 app copy 在 `export_transcript` 成功返回可复制文本后写入 macOS pasteboard，export 通过 `NSSavePanel` 获取用户确认目标；XCTest 继续注入 memory clipboard 和 deterministic target，architecture guard 限定该 OS boundary 不得调用 process、provider internals、network API、`NSOpenPanel`、直接 file mutation/delete 或自动下载。`OD-MA-009` 当前保持 watch：自动 GPT/Qwen 纪要未进入范围，现有证据只能覆盖手动复制/导出边界。阻塞缺口：`TDG-MA-004` copy/export 用户触发、冻结响应消费、受控 app-bundle process-runner workspace、production action client 默认 process runner 和 production OS clipboard/save-panel boundary 证据得到 partial closure；2026-07-05 real action app-bundle smoke 已证明用户点击 copy/export 能经 provider-owned CLI 路由执行 `export_transcript`、返回 `Copy complete.` / `Export complete.` 并保留导出文件；2026-07-05 VS-MA-20 opt-in controlled MVP full-stack app-bundle smoke 已补同一 workspace/session 的阶段链路。但仍不证明真实 native capture、真实 runtime/model 质量、真实用户 Save Panel、release bundle 或 release-scope full-stack。关闭条件：生产范围 action command bridge、安全检查和 release-scope app bundle 真实 capture/action 链路证据进入标准门禁后再考虑 `covered`；如自动纪要进入范围，必须新增 API/security/data 契约和对应 `PV-MA-*` 覆盖 | `partial` |
| PV-MA-012 | `CAP-MA-012` 删除当前 workspace 内目标会话目录，且不删除 workspace 外导出文件 | `docs/product-spec/03-permissions-and-identity.md`, `docs/product-spec/06-api-contracts.md`, `docs/product-spec/07-data-and-events.md`, `docs/product-spec/13-security-and-compliance.md` | 本地命令契约测试、路径边界测试、文件删除 fixture 测试、UI 确认状态测试 | `platform/processing-cli/scripts/test.sh`; `platform/processing-cli/tests/test_cli_contract.py`; `platform/processing-cli/tests/test_delete_session.py`; `platform/native-app/scripts/test.sh`; `platform/native-app/tests/MeetingAssistantNativeTests/TranscriptReviewActionsViewModelTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeUITests/NativeControlPlaneSmokeTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeAppUITests/AppBundleLocatorSmokeTests.swift`; `platform/e2e/smoke-test.sh`; 后续 native-to-processing command bridge 和 release bundle checks | 当前证据：`processing-cli` 已实现 `delete_session`，组件测试覆盖 `confirm=true` 删除摘要、`confirm=false` `invalid_input` exit code、目标不存在 `not_found`、session root symlink escape `path_conflict`、session root symlink 指向 workspace sibling session 时 fail-closed 且不删除目标、`sessions/` 目录 symlink fail-closed、workspace `events/` symlink fail-closed、删除事件文件 symlink/hardlink fail-closed、session 内 symlink 只删除链接不删除外部目标、workspace 外导出文件保留、locked session `path_conflict` 且不删除、损坏 `session.json` 返回稳定 `processing_failed` 且不删除、workspace 级 `meeting_session.deleted.v1` 删除事件摘要、permission failure `permission_denied`、CLI success/unknown arg/confirm exit code；Line B 进一步强化 delete provider 契约，覆盖 `not_found` exit 3、`confirm=false` 保留会话、workspace 外 export 保留、`deleted_items` 只含相对应用管理路径、response/event 不携带 transcript 内容、成功路径无外部网络或子进程副作用，以及缺失 `--session-id` required-arg matrix。`test_cli_contract.py` 覆盖缺失 `--confirm`、非法 confirm enum、unknown argument、非冻结 `--format` 参数拒绝、`../outside` session id path traversal fail-closed、单 JSON failure shape 和 exit code 2。P2-C local smoke 先断言 `confirm=false` 保留会话，再在完整处理流水线后执行 `delete_session confirm=true`，断言 workspace 内 session 目录删除、workspace 外 export 保留、`deleted_items`/`retained_external_exports` 摘要和 workspace 级 `meeting_session.deleted.v1` 事件结果一致且不携带 transcript 内容。Line A 增加 native delete confirmation 入口；Swift Testing 覆盖 request/cancel/confirm、取消不发送 delete、成功/失败摘要、缺失字段和 invalid response，hosted smoke 与 app-bundle XCUITest 覆盖 `ma.transcriptAction.deleteButton`、`ma.transcriptAction.deletePrompt`、`ma.transcriptAction.deletePromptText`、`ma.transcriptAction.deleteConfirmButton`、`ma.transcriptAction.deleteCancelButton`、`ma.transcriptAction.success`、`ma.transcriptAction.error` 等确认/取消 locators。本分支新增 Debug/XCTest-only `MA_NATIVE_TRANSCRIPT_ACTION_CLIENT=process` app-bundle smoke，通过 native-owned fixture 驱动 `TranscriptActionProcessRunner`，断言用户确认 delete 后真实临时 session 目录被删除、导出文件保留、delete invocation 使用冻结 flags，且非 JSON stderr bridge failure 不泄漏路径或自由文本。阻塞缺口：`TDG-MA-004` delete 确认/取消、冻结响应消费、受控 app-bundle process-runner workspace 删除和 production action client 默认 process runner 证据得到 partial closure；2026-07-05 real action app-bundle smoke 已证明用户确认 delete 能经 provider-owned CLI 删除 session root、保留 workspace 外导出，并验证 delete event 不携带 transcript 文本；2026-07-05 VS-MA-20 opt-in controlled MVP full-stack app-bundle smoke 已补同一 workspace/session 的阶段链路。但仍不证明 release-scope 真实 OS 级删除集成、真实 native capture、真实 runtime/model 质量、release bundle 或 release-scope full-stack。关闭条件：生产范围 delete action bridge、真实删除边界、失败摘要和 release-scope app bundle 真实 capture/action 链路证据进入标准门禁后再考虑 `covered` | `partial` |

| PV-MA-013 | `CAP-MA-013` 设计化原生 app shell 覆盖预检、录制、artifacts、处理、transcript、导出和删除，并触发现有 command/helper/adapter 契约 | `docs/product-spec/04-user-journeys-and-ui.md`, `docs/product-spec/09-acceptance-criteria.md`, `docs/product-spec/12-ui-ux-design.md` | Native designed shell regression、app-bundle XCUITest、Swift Testing、Debug/Release fixture 隔离检查、可选截图证据 | `platform/native-app/scripts/test.sh`; `platform/native-app/scripts/test-app-bundle.sh`; `platform/native-app/tests/MeetingAssistantNativeTests/DesignedNativeShellViewModelTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeUITests/NativeControlPlaneSmokeTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeAppUITests/DesignedNativeShellAppBundleTests.swift`; `platform/native-app/UITests/MeetingAssistantNativeAppUITests/AppBundleLocatorSmokeTests.swift`; `platform/e2e/native-transcript-bridge-smoke.sh`; `platform/e2e/full-stack-smoke.sh`; `./scripts/agent-workflow-check.sh`; `./scripts/review-report.sh` | 当前证据：`VS-MA-19A` 已新增 `DesignedNativeShellViewModel.swift` 和 `DesignedNativeShellView.swift`，由 `MeetingAssistantNativeApp.swift` 承载，同一 SwiftUI shell 覆盖 Preflight、recording、session artifacts、processing、transcript、copy/export/delete；主操作复用既有 recording、processing 和 transcript action view model/client，不在 shell 本地伪造成功。`DesignedNativeShellViewModelTests.swift` 覆盖 section 顺序、accessibility locator、status/artifact/processing projection；`NativeControlPlaneSmokeTests.swift` 覆盖 hosted shell、源码 locator contract，并断言 app-bundle Release hooks 保持隔离，Debug/XCTest fixture 不会成为 Release 默认行为；`DesignedNativeShellAppBundleTests.swift` 在真实 app bundle 下覆盖稳定 navigation/status/section locator、blocked/degraded 状态，以及 start/stop recording、artifact 状态、processing、copy/export/delete 主操作链路；`AppBundleLocatorSmokeTests.swift` 继续证明旧 command/helper/adapter fixture 路径未被 shell 替换破坏。VS-MA-20 bridge smoke 复用 processing-cli 临时 workspace/session artifacts，并在 SwiftPM harness 中断言 `TranscriptReviewWorkspaceLoader`/`TranscriptReviewViewModel` 能消费同一 workspace，同时验证 `MeetingAssistantNativeApp.swift` app root 挂载 `DesignedNativeShellView` 而非 primitive debug UI、shell section 顺序和 `ma.shell.*` locator；`platform/e2e/full-stack-smoke.sh` 已把该 bridge 作为 manifest full-stack 阶段 marker。2026-07-04 已执行 `./platform/native-app/scripts/test.sh` 通过；2026-07-04 已执行 `./platform/native-app/scripts/test-app-bundle.sh` 通过 22 个 app-bundle XCUITest、1 个按 opt-in 条件跳过、0 失败。关闭口径：`AC-MA-013` 要求的 designed shell、状态层级、主操作 command/client wiring、primitive debug UI 替换和 Debug/Release fixture 隔离已由标准 native component gate 与显式 app-bundle gate 覆盖。限制：本行只关闭设计化 shell 口径，不证明真实 capture、真实 processing provider、真实 OS pasteboard/file picker/delete integration、真实 release distribution bundle 或其他 `PV-MA-*`；这些仍按各自行单独关闭 | `covered` |

## 2026-06-30 集成证据回填

本节补充上方 `PV-MA-*` 行的阶段证据，不新增产品事实，不改变 command、artifact、event、error code、exit code 或 UI state contract。除上方已经按行级关闭条件推进到 `covered` 的条目外，本节涉及的行继续保持 `partial`，直到真实 native capture、真实 native-to-processing 触发、真实 runtime、release bundle 和发布范围 E2E 关闭对应缺口。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| LineB `4234779`，集成提交 `ea1aee5` | `PV-MA-006`、`PV-MA-007`、`PV-MA-008`、`PV-MA-009`、`PV-MA-010`; `TDG-MA-002`、`TDG-MA-003`、`TDG-MA-006`、`TDG-MA-007` | `platform/processing-cli` 强化 `session.json`、`transcript.json`、`speaker_labels.json` 的 managed-output/temp 写入边界；测试覆盖 symlink、hardlink、temp path、rollback cleanup、外部目标不写入和失败不登记 artifact；`platform/e2e/smoke-test.sh`、`native-transcript-bridge-smoke.sh`、`full-stack-smoke.sh` 增加 regular-file、checksum、no-temp-leftover、response marker 和 artifact 可读性断言；LineB 报告中 `./scripts/test-e2e-full-stack.sh` 在本地 cached `meeting-assistant-smoke:local` 可见后重跑通过 | 只强化 provider artifact smoke 和 processing-to-native workspace artifact bridge 证据；不证明 native UI 真实触发 processing、真实 capture、真实 runtime、release bundle 或任意同用户 TOCTOU 全消除；相关行保持 `partial` |
| LineA `1b531f2`，集成提交 `315663e` | `PV-MA-006`、`PV-MA-007`、`PV-MA-008`、`PV-MA-009`; `TDG-MA-004`，并补充 `TDG-MA-001` consumer-side response decode 证据 | `platform/native-app` 增加 `ProcessingCommandClient`、fake client、生产 process runner 注入点、`ProcessingStateViewModel`、`ProcessingStateView` 和 `ma.processing.*` locator；Swift Testing、hosted smoke 与 app-bundle XCUITest 覆盖 blocked、running、completed、transcript-only degraded、failed 和 retry 状态；Integration PM 合入后将 native fake 成功态 `label_status` 对齐 provider 枚举 `labeled`，保留 `transcript_only` 降级分支 | 证明 native consumer 可以消费冻结 processing response 并呈现处理状态；不证明真实 native capture、生产 app 真实调用 processing-cli、真实 provider 质量、真实 diarization、真实 runtime 或 release bundle；`PV-MA-006`-`PV-MA-009` 保持 `partial` |

## 2026-07-01 集成证据回填

本节补充 LineB / E2E smoke reliability 与 LineA / guarded native processing fixture evidence 的集成证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、UI state 或 release 范围；所有涉及 `PV-MA-*` 继续保持 `partial`，不能声明真实 native capture、真实 runtime、生产 app 真实调用 processing provider、copy/export/delete 的 release bundle 或发布候选已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| LineB `6689831`，集成提交 `f978498` | `PV-MA-006`、`PV-MA-007`、`PV-MA-008`、`PV-MA-009`、`PV-MA-010`、`PV-MA-011`、`PV-MA-012`; `PV-HARNESS-007`; `TDG-MA-003`、`TDG-MA-006`、`TDG-MA-007` | `platform/e2e/prepare-smoke-image.sh` 增加 Docker daemon preflight、target/base image candidates、probe attempts 输出，并用 `docker run --pull=never --network none` 验证本地 smoke image 可运行；`full-stack-smoke.sh` 增加 processing/native bridge 阶段归因、exit code 和 marker 缺失诊断；`smoke-test.sh` 隔离 deterministic fake/local runtime 与 model env，并补 no-auto-download、delete path traversal、export conflict no-overwrite、relative `deleted_items`、delete event 不携带 transcript 内容等断言；`native-transcript-bridge-smoke.sh` 补 Swift 缺失可解释错误、transcript segment id/timestamp/order、speaker fallback degradation、session/transcript identity 和 Swift 读取前后 artifact checksum 不变断言。LineB 报告中 `platform/processing-cli/scripts/test.sh`、`platform/e2e/full-stack-smoke.sh` 和 `MEETING_ASSISTANT_SMOKE_IMAGE=meeting-assistant-smoke:local ./scripts/test-e2e-full-stack.sh` 在本地 cached smoke image 可见后通过 | 只强化 processing local smoke、processing-to-native bridge smoke、full-stack smoke image precondition 与失败可诊断性；保持 no-auto-pull/no-auto-download/no-auto-upload 边界。该证据不证明 native UI 真实触发 processing、真实 native capture、真实 release bundle、真实 speaker diarization 质量、所有 runtime 环境稳定性或所有同用户 TOCTOU 场景关闭；相关行保持 `partial` |
| LineA `60f1eda`，集成提交 `8a5bfda` | `PV-MA-006`、`PV-MA-007`、`PV-MA-008`、`PV-MA-009`; `TDG-MA-001` consumer-side response decode；`TDG-MA-004` native processing state UI | `platform/native-app` 增加 guarded `MA_NATIVE_PROCESSING_CLIENT=process` process-runner fixture evidence：Release build 忽略该 env hook 并回退 fake client，Debug build 仍必须带 XCTest/test marker 才能注入 `ProcessingCommandProcessRunner`；Xcode Debug 配置显式设置 `SWIFT_ACTIVE_COMPILATION_CONDITIONS = DEBUG`，app-bundle UI test fixture 注入 `MA_NATIVE_APP_XCTEST=1`。native-owned `processing-command-fixture.sh` 输出冻结 CMD-MA-005/CMD-MA-006 stdout JSON；Swift Testing 与 app-bundle XCUITest 覆盖 completed、transcript-only degraded、failed 和 retry 的 `ProcessingStateViewModel` / `ma.processing.*` locator。Integration PM 合入前已将 commit message 改为 `test(native-app): add guarded processing runner fixture evidence`，避免把证据误读为真实 provider 或 release coverage | 该证据只证明 controlled env-only/native-owned fixture 可通过冻结 stdout JSON 驱动 native processing state consumer；它不证明 release build 会启用该 hook、不证明真实 processing provider、真实 transcription runtime、真实 capture、真实 diarization quality、native 触发真实 processing、release bundle 或任意 `PV-MA-*` covered。矩阵文案和后续交付必须继续使用 controlled fixture / partial closure 口径 |
| LineB `ac09045`，集成提交 `cbeb4b4` | `PV-MA-002`、`PV-MA-003`、`PV-MA-006`、`PV-MA-007`、`PV-MA-008`、`PV-MA-009`、`PV-MA-011`、`PV-MA-012`; `TDG-MA-002`、`TDG-MA-003`、`TDG-MA-006`、`TDG-MA-007` | `platform/e2e/capture-processing-smoke.sh` 新增 capture-style processing smoke：通过 test-only `source_type=native_recording`、`status=recorded` workspace fixture 和既有 artifact contract，验证 capture 产物形态可进入 `generate_transcript`、`generate_speaker_labels` transcript-only fallback、`export_transcript` 和 `delete_session`；覆盖 checksum、regular-file/path boundary、symlink rollback、`degradation_reason`、no-auto-download、external export retention、relative `deleted_items` 和 delete event 不携带 transcript 内容。该 smoke 已接入 `platform/processing-cli/scripts/test.sh` 和 `platform/e2e/full-stack-smoke.sh` | 只证明 processing/e2e 可消费 capture-style artifacts 的 partial evidence；不证明真实 native capture、native UI 真实触发 processing、真实 transcription runtime、真实 diarization quality、release bundle 或任意 `PV-MA-*` covered。root Docker full-stack gate 仍依赖本地 cached smoke image 前置条件，脚本保持 no-auto-pull |
| LineA `0833132`，集成提交 `c809510` | `PV-MA-002`、`PV-MA-003`、`PV-MA-009`; `TDG-MA-004`、`TDG-MA-005` | `platform/native-app` 增加 controlled native capture adapter、`NativeRecordingCommandClient`、`RecordingSessionStore`、permission fail-closed、start/stop 状态和 stop 后 artifact registration；Debug/XCTest-only `MA_NATIVE_RECORDING_CLIENT=controlled` hook 写入 `session.json`，登记 `screen_video`、`system_audio`、`microphone_audio`、`mixed_audio`，包含 checksum、`capture_status` 和 `degradation_reason`。native lint/test/architecture/security/build 在 LineA worktree 通过，Integration PM 只读复核 `architecture.sh`、`security.sh` 和 `git diff --check` 通过 | 只证明 controlled Debug/XCTest native capture artifact registration partial evidence；不证明真实 macOS capture framework、真实 TCC/system-audio capability、app restart recovery、processing provider/full-stack release closure、release bundle 或任意 `PV-MA-*` covered。若后续需要辅助 capture provider，必须先走 spec-change/ADR |
| LineB `df6589d`，集成提交 `168b011` | `PV-MA-006`、`PV-MA-007`、`PV-MA-008`、`PV-MA-009`、`PV-MA-011`、`PV-MA-012`; `PV-HARNESS-007`; `TDG-MA-002`、`TDG-MA-003`、`TDG-MA-006`、`TDG-MA-007` | `platform/processing-cli` 增加 runtime/provider hardening：`whisper_cpp` timeout、bad output、missing output、失败不覆盖已有有效 transcript、失败响应和 processing log free-text 脱敏、audio/transcript/speaker fallback 回滚和诊断边界；`platform/e2e/capture-processing-smoke.sh` 补充 capture-style normalize -> transcript -> speaker fallback -> export -> delete 的 lock/path_conflict、temp hardlink rollback、checksum drift、export/delete 不泄漏 transcript 内容等断言。Integration PM 复核 `PYTHONPATH=platform/processing-cli/src python3 -m unittest discover -s platform/processing-cli/tests` 通过 171 tests，`platform/processing-cli/scripts/test.sh` 通过并输出 `whisper.cpp smoke passed.`、`p2-c processing local e2e smoke passed.`、`capture-style processing e2e smoke passed.`；`architecture.sh`、`security.sh`、`build.sh`、`platform/e2e/capture-processing-smoke.sh` 和 `platform/e2e/full-stack-smoke.sh` 均通过 | 证明当前集成机器上真实 `whisper.cpp` mixed-language smoke 实际通过，并强化 provider/E2E 的异常、回滚、脱敏和 no-auto-download/no-auto-upload/no-auto-pull 证据；不外推到所有开发机 runtime/model/fixture 可用性，不证明真实 diarization quality、native UI 真实触发 processing、release bundle 或任意 `PV-MA-*` covered；相关行保持 `partial` |
| LineA `df1d11d`，集成提交 `ffe071b` | `PV-MA-002`、`PV-MA-003`、`PV-MA-009`; `TDG-MA-004`、`TDG-MA-005` | `platform/native-app` 新增 bounded `AppleScreenCaptureKitNativeCaptureAdapter` spike，唯一允许在 native-app 内使用 Apple 官方 `ScreenCaptureKit` / `AVFoundation` capture API 的文件；architecture guard 限定 Apple capture framework 只能出现在该文件，并继续禁止 OBS、BlackHole、FFmpeg 辅助 capture、helper/processing-cli、网络、真实 pasteboard、file picker 和 store-owned metadata/checksum 写入。该 adapter 仅支持 screen target，combined recording file 登记为既有 `screen_video`，不可证明的 `system_audio`、`microphone_audio`、`mixed_audio` 通过既有 artifact registry 写入 `degraded` 或 `missing` 和 `degradation_reason`。Integration PM 复核 native lint/architecture/security/build/test 均通过，app-bundle XCUITest 14/0 通过；Release env hook 未启用 `apple_screencapturekit`，测试 env hook 仍限 Debug/XCTest | 只证明 Apple 官方 ScreenCaptureKit adapter 的编译级、单测级和 bounded architecture spike，以及 controlled app-bundle artifact registration 证据；不证明真实 ScreenCaptureKit recording output 已被实际录制、不证明真实系统音频、真实 TCC 负向路径、生产 app 使用 Apple adapter 录制、native UI 真实触发 processing、release bundle 或任意 `PV-MA-*` covered。若后续需要辅助 capture provider，仍必须先走 spec-change/ADR |

## 2026-07-02 集成证据回填

本节补充 VS-MA-21 / VS-MA-22 LineB provider failure redaction、异常路径和安全供应链证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、UI state 或 release 范围；所有涉及 `PV-MA-*` 和 `SEC-MA-*` 继续保持 `partial` 口径，不能声明真实 native capture、native UI 真实触发 processing、release bundle、真实 diarization quality、model provenance/hash/license 或所有开发机 runtime/model 可用性已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| LineB `669ef91`，集成分支 `codex/integration-vs-ma-21-22-provider-security` | `PV-MA-005`、`PV-MA-006`、`PV-MA-007`、`PV-MA-009`、`PV-MA-011`、`PV-MA-012`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-002`、`TDG-MA-006`、`TDG-MA-007` | `platform/processing-cli` 进一步约束 provider failure redaction：normalizer、transcript adapter 和 speaker adapter 的 provider `ContractError` message 使用稳定通用文案，provider-owned details 默认不可信；`details.path` 不再透传；allowlisted details 只保留 per-key 已知安全 primitive 值，nested map/list、路径、secret、transcript-like 任意后缀和单词型自由文本统一 `<redacted>`；CLI parse failure 对未识别首参固定 `command=unknown` 并脱敏 argparse error。组件测试新增或强化 provider `details.path`、allowlisted free-text、nested map/list、runtime=`ProjectApolloRoadmap`、language=`secretcodename`、arbitrary artifact/transcript id、fallback enabled/disabled、processing.log 不泄漏、temp rollback 和 artifact 不生成断言；dependency tests 覆盖 non-executable runtime、missing/directory model 和 English-only `.en` model 拒绝。Integration PM 复核 `PYTHONPATH=platform/processing-cli/src python3 -m unittest discover -s platform/processing-cli/tests` 通过 181 tests，`platform/processing-cli/scripts/test.sh` 通过并输出 `whisper.cpp smoke passed.`、`p2-c processing local e2e smoke passed.`、`capture-style processing e2e smoke passed.`；`platform/processing-cli/scripts/security.sh`、`sbom.sh`、`architecture.sh`、`build.sh`、`platform/e2e/capture-processing-smoke.sh` 和 `platform/e2e/full-stack-smoke.sh` 均通过 | 证明当前集成机器上 provider failure response/log 的自由文本、路径和 secret 泄漏风险得到自动化回归覆盖，并补强 no-auto-download/no-auto-upload、本地 runtime/model fail-closed 和组件级 security/SBOM/architecture/build 证据；不证明完整 SAST/SCA/license/provenance/release coverage，不证明真实 native capture、native UI 真实触发 processing、release bundle、真实 speaker diarization 质量、model provenance/hash/license 或所有开发机 runtime/model 可用性；相关行保持 `partial`，VS-MA-23 仍需 release-scope evidence 后才能启动 |

## 2026-07-03 集成证据回填

本节补充 VS-MA-21 LineB provider/boundary failure fixture 与 LineA native consumer retry bridge 的集成证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、UI state 或 release 范围；所有涉及 `PV-MA-*` 和 `SEC-MA-*` 继续保持 `partial` 口径，不能声明真实 native capture、native UI 真实触发 processing、release bundle、真实 diarization quality、model provenance/hash/license 或所有开发机 runtime/model 可用性已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| LineB `dd65cb7`，LineA `ee3c1d6`，集成分支 `codex/integration-vs-ma-21-native-provider-hardening` | `PV-MA-005`、`PV-MA-006`、`PV-MA-007`、`PV-MA-008`、`PV-MA-009`; `SEC-MA-001`、`SEC-MA-003`、`SEC-MA-004`; `TDG-MA-002`、`TDG-MA-004`、`TDG-MA-006`、`TDG-MA-007` | LineB 在 `platform/e2e/capture-processing-smoke.sh` 增加 VS-MA-21 provider failure non-contract marker 和 capture-style runtime failure fixture：`generate_transcript --runtime whisper_cpp` 的 `dependency_missing` exit 4 断言无 `normalized_audio` / `transcript_text` 污染、capture checksum 不变、无 temp leftover 和 failure surface 脱敏；fake `whisper.cpp` runtime 非零失败断言 `processing_failed` exit 5、response/log 不泄漏 provider sensitive text、transcript-like text、runtime/model/session/artifact path，失败后 retry success 并复用 normalized audio source。LineA 在 `platform/native-app` 增加 native consumer hardening：`ProcessingCommandProcessRunner` 对无 JSON stdout 的 bridge failure 只映射既有 frozen error code，不展示 stderr/stdout 原文；`ProcessingStateViewModel` 对 failure message/details/warnings 和 transcript-only degradation reason 做 safe display，保留 `path_conflict`、`dependency_missing`、`processing_failed` 等既有 code；retry 重放上一轮请求，busy 状态阻止重复 start/retry，readiness blocked 时 fail closed；app-bundle XCUITest 覆盖安全错误展示和 retry locator。Integration PM 合并顺序为 LineB -> LineA，合并预检无文件冲突，LineB/LineA `git diff --check` 均通过 | 证明 provider failure fixture、failure response/log redaction、derived artifact no-pollution、retry success、native safe-display、native retry/busy/readiness guard 和 app-bundle locator 得到自动化证据；不改变公开 CLI/command/artifact/error/exit/UI contract，不证明真实 native capture、生产 app 真实触发 processing provider、真实 transcription runtime 在所有机器可用、真实 diarization quality、release bundle、完整 SAST/SCA/license/provenance 或任意 `PV-MA-*` covered。相关行继续保持 `partial`；VS-MA-23 仍需 release-scope evidence 后才能启动 |

## 2026-07-03 VS-MA-23 readiness 集成证据回填

本节补充 VS-MA-23 Provider/E2E release readiness 与 native app-bundle readiness 的集成证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、UI state 或 release 范围；所有涉及 `PV-MA-*` 和 `SEC-MA-*` 继续保持 `partial` 口径，不能声明真实 native capture、生产 app 真实触发 processing provider、release bundle、真实 diarization quality、model provenance/hash/license 或所有开发机 runtime/model 可用性已 covered。`./scripts/product-validation-check.py release` 仍按预期失败，说明本轮是 release-readiness evidence 收敛，不是 release candidate 放行。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| LineB `ce071e3`，LineA `ba7e9b2`，集成分支 `codex/integration-vs-ma-23-readiness-06ee4a8` | `PV-MA-001`-`PV-MA-012`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-004`、`TDG-MA-005`、`TDG-MA-006`、`TDG-MA-007` | LineB 新增 `platform/processing-cli/scripts/release-provider-smoke.sh`，要求显式 `whisper.cpp` runtime、multilingual model 与 fixture audio env，验证 `.local` asset path 边界、large-v3 模型名、`whisper.cpp smoke passed.` marker、no-auto-download/no-auto-upload 口径；同时增强 processing-cli `security.sh`、`sbom.sh`、`architecture.sh`、`build.sh` 的 release evidence 输出，并在 `platform/e2e/full-stack-smoke.sh` 增加 VS-MA-23 non-contract release readiness summary。LineA 增强 app-bundle XCUITest 控件 hittability/scroll 稳定性，并强化 native architecture/build 检查：Release configuration 不能启用 Debug/XCTest-only processing/capture hooks，Release build artifact 不包含 test hook env marker，Debug/XCTest hook 仍限测试路径。Integration PM 按 LineB -> LineA 合并，无冲突；复核 `platform/processing-cli/scripts/test.sh`、`platform/native-app/scripts/test.sh`、processing/native architecture/security/build、processing SBOM、`platform/processing-cli/scripts/release-provider-smoke.sh`、`platform/e2e/capture-processing-smoke.sh`、`platform/e2e/full-stack-smoke.sh`、`./scripts/project-manifest-check.sh current`、`./scripts/architecture-check.sh`、`./scripts/security-check.sh`、`./scripts/supply-chain-check.sh current` 均通过；此前 LineB root `check.sh` 暴露的 4 个 native app-bundle failures 在 integration 分支的 `platform/native-app/scripts/test.sh` 中均通过 | 证明 VS-MA-23 provider/e2e release-readiness markers、native app-bundle processing failure/retry、bridge failure safe display、processing env success/degradation/failure、ready fixture start/stop、transcript action smoke、Release/Debug hook separation 和组件级 security/SBOM/architecture/build 证据得到集成验证。该证据仍不关闭 release candidate：`./scripts/product-validation-check.py release` 仍显示 `covered=10, partial=12`；真实 macOS 权限负向、真实 native capture 产物链、生产 app 真实触发 processing provider、真实文件/OS 集成、完整 release bundle、完整 SAST/SCA/license/provenance 和所有发布范围 `PV-MA-*` covered 仍未证明；相关行继续保持 `partial`，`release-preflight.sh` 仍不应作为放行证据运行或报告通过 |
| LineA `c63f0b4`，LineB `52cd548`，LineC `218fb2f`，LineD0 `529d75c`，LineE `0fd58b2`，集成分支 `codex/integration-vs-ma-23-readiness-06ee4a8` | `PV-MA-001`-`PV-MA-012`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-004`、`TDG-MA-005`、`TDG-MA-006`、`TDG-MA-007` | LineB 将 `release-provider-smoke.sh` 调整为 release fail-closed：除显式 runtime/model/audio 和 `.local`/large-v3/multilingual/no-auto-download 边界外，还要求 model sha256、license 和 provenance sidecar，`platform/processing-cli/scripts/test.sh` 覆盖缺失 sidecar blocker 并在本机通过 181 个组件测试、`whisper.cpp smoke passed.`、P2-C local smoke 和 capture-style smoke。LineA 增加可注入 `CGPreflightScreenCaptureAccess` screen-recording 权限探针和 macOS permission checker，Swift Testing 覆盖真实 macOS screen-recording preflight denied 时 fail closed、不启动 capture adapter、不写 session，以及 microphone permission unknown 只在请求 microphone capture 时阻断；同时覆盖 stop 时 adapter 省略 artifact 会登记为 `missing` 并写入 `degradation_reason`。LineC 增强 native-owned `processing-command-fixture.sh`，在受控 process-runner fixture 中写出 workspace `session.json`、`transcript.json` 和 `speaker_labels.json`，app-bundle XCUITest 证明处理完成后同一临时 workspace 可被 transcript review loader 读取并显示 timestamp、文本和匿名 speaker label。LineD0 增加 `TranscriptActionProcessRunner` 和响应 decoder，只使用冻结 `export_transcript`/`delete_session` flags，不新增 `--format`，非 JSON 或无 stdout failure 映射为安全错误文案；architecture guard 将 `Process`/`Pipe` 限定在该 runner 文件，真实 pasteboard、file picker、direct delete、网络和 provider internals 仍禁止，Swift Testing 覆盖 frozen arguments、structured failure、safe bridge error、注入 clipboard/destination 和 command delete。LineE 将 `review-report.sh --require-release-evidence` 收紧到 `.harness/evidence/release`，要求 release-scope `production-readiness`、`docs`、`check`、`mocked-e2e`、`full-stack-e2e`、`supply-chain` evidence steps，并用 harness unittest 覆盖 check-scope evidence、readiness-only evidence 和 partial product validation 不能冒充 release evidence | 本轮证明 release provider smoke 能对 model hash/license/provenance 缺口 fail-closed，native capture permission/artifact registration 增加真实 macOS TCC preflight 可注入证据，controlled processing workspace artifact 可从 app-bundle processing path 进入 transcript review，transcript action CLI bridge 的公开命令参数和安全错误边界已自动化覆盖，release evidence gate 不再接受阶段或 check-scope 证据冒充发布证据。状态仍保持 `partial`：这些证据不证明真实 ScreenCaptureKit 输出、真实系统音频、生产 app 默认启用真实 native capture 或真实 processing provider、真实 OS pasteboard/file picker/delete integration、完整 release bundle、完整 SAST/SCA/license/provenance sidecar 已准备齐全或所有发布范围 `PV-MA-*` covered；`./scripts/product-validation-check.py release` 仍应失败，`release-preflight.sh` 仍不得作为放行证据报告通过 |
| 本分支 `codex/vs-ma-23-release-blockers` | `PV-MA-011`、`PV-MA-012`; `TDG-MA-004` | `platform/native-app` 增加 Debug/XCTest-only `MA_NATIVE_TRANSCRIPT_ACTION_CLIENT=process` app-bundle hook，默认和 Release 仍回退 `TranscriptActionFakeCommandClient`；native-owned `test-fixtures/transcript-action-command-fixture.sh` 通过冻结 action stdout JSON 驱动 `TranscriptActionProcessRunner`，app-bundle XCUITest 在临时 workspace 中用户触发 copy/export/delete，断言 copy/export/delete invocation、真实导出 Markdown 文件存在、确认删除后 session 目录被移除且导出文件保留，并覆盖非 JSON stderr bridge failure 只显示安全错误文案、不泄漏 stderr 路径或自由文本。native lint/test/architecture/build gate 同步登记 fixture、hook 和 Release/Debug 边界 | 该证据把 transcript action app-bundle smoke 从 fake-only 推进到受控 process-runner workspace 证据；仍不证明生产 app 默认启用真实 action process、真实 macOS pasteboard、真实文件选择器、真实 OS 级删除集成、真实 processing provider、真实 capture、完整 release bundle 或任意 `PV-MA-*` covered。`PV-MA-011`、`PV-MA-012` 继续保持 `partial`；`./scripts/product-validation-check.py release` 仍应失败 |

## 2026-07-04 VS-MA-14/15 opt-in native capture smoke 证据回填

本节补充真实 ScreenCaptureKit native capture smoke 入口证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、UI state、Release hook 或发布范围；所有涉及 `PV-MA-*` 和 `SEC-MA-*` 继续保持 `partial` 口径，不能声明真实 native capture、生产 app 默认真实录制、native UI 真实触发 processing provider、release bundle 或所有开发机 macOS/TCC 环境已 covered。

2026-07-04 本机实跑 `MA_NATIVE_CAPTURE_SMOKE=1 ./platform/native-app/scripts/native-capture-smoke.sh` 通过：临时 Swift executable 构建成功，`session-native-capture-smoke-20260704T042846Z-73008` 完成 2 秒 screen-only ScreenCaptureKit 录制，`screen_video.mp4` 为 available，大小 362417 bytes，checksum 为 `sha256:7004a49df82c98df6c123728a69b30f5a9d3b4db52d1cffed1b8fe4e1dd88e66`，`session.json` 校验无错误；`system_audio`、`microphone_audio` 和 `mixed_audio` 按默认未请求策略记录为 missing。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本分支 `codex/vs-ma-14-15-native-capture-smoke-port` | `PV-MA-001`、`PV-MA-002`、`PV-MA-003`、`PV-MA-009`; `TDG-MA-002`、`TDG-MA-005`、`TDG-MA-006`、`TDG-MA-007` | `platform/native-app/scripts/native-capture-smoke.sh` 新增显式 `MA_NATIVE_CAPTURE_SMOKE=1` opt-in 真实 native capture smoke：临时构建 Swift executable，组合既有 `NativeRecordingCommandClient`、`MacOSNativeCapturePermissionChecker`、`AppleScreenCaptureKitNativeCaptureAdapter` 和 `RecordingSessionStore`，默认只请求屏幕录制，写入 `build/native-capture-smoke/` 或显式 workspace，成功时验证既有 `session.json`、`screen_video` available、非空录制文件和 `sha256:` checksum；权限 denied/unknown、macOS runtime 不足、display/no-output 或 ScreenCaptureKit stop failure 均保持 fail closed。`NativeRecordingCommandClient` 同步补强 start 后 `RecordingSessionStore` 失败时 best-effort stop adapter，`AppleScreenCaptureKitNativeCaptureAdapter` 不再把临时 `combined_recording.mp4` 路径写进用户可见错误。native lint/test/architecture/security gate 登记脚本、capability 和 opt-in 边界，但不把真实 capture 加入默认组件测试或 Release 门禁 | 该证据把 Apple ScreenCaptureKit spike 从编译/单测级推进到可显式运行的本机真实录制 smoke 入口，并补强启动失败清理和临时路径脱敏。状态仍保持 `partial`：不证明所有开发机 macOS/TCC/display 环境可用、独立系统音频或麦克风音频产物、生产 app 默认启用真实 adapter、native UI 真实触发 processing provider、release bundle 或任意 `PV-MA-*` covered。真实 smoke 运行失败只能作为 fail-closed 诊断；成功也仍为 partial，直到发布范围标准门禁和关闭条件完整覆盖 |

## 2026-07-04 VS-MA-20 opt-in app-bundle real capture UI smoke 证据回填

本节补充 designed native shell 通过真实 `.app` Start/Stop 按钮触发 Apple ScreenCaptureKit adapter 的 opt-in 证据入口。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、UI state、Release hook 或发布范围；所有涉及 `PV-MA-*` 和 `SEC-MA-*` 继续保持 `partial` 口径，不能声明生产 app 默认真实录制、native UI 真实触发 processing provider、release bundle 或所有开发机 macOS/TCC 环境已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本分支 `codex/vs-ma-20-real-capture-app-bundle-smoke` | `PV-MA-001`、`PV-MA-002`、`PV-MA-003`、`PV-MA-009`、`PV-MA-013`; `TDG-MA-004`、`TDG-MA-005`、`TDG-MA-007`、`TDG-MA-008` | `MeetingAssistantNativeApp` 新增 Debug/XCTest-only `MA_NATIVE_RECORDING_CLIENT=apple_screencapturekit` hook，且必须同时满足 `MA_NATIVE_CAPTURE_SMOKE=1` 和 recording workspace 才会选择 `AppleScreenCaptureKitNativeCaptureAdapter`；Release 和默认测试继续回落 `FakeRecordingCommandClient`。`AppBundleLocatorSmokeTests.testOptInAppleScreenCaptureKitRecordsFromDesignedShellWhenExplicitlyEnabled` 默认 `XCTSkip`，只有显式 opt-in 才从 designed shell 点击 `ma.recording.startButton`/`ma.recording.stopButton`，screen-only 录制后校验 UI saved 状态、`screen_video` available、音频 artifact missing、`session.json`、非空录制文件和 `sha256:` checksum。`platform/native-app/scripts/test-app-bundle.sh` 新增 `MA_NATIVE_APP_REAL_CAPTURE_SMOKE=1` 分支：先 `build-for-testing`，再把 opt-in env 写入 `.xctestrun`，最后 `test-without-building -only-testing` 跑单用例，避免直接 shell env 被 XCUITest runner 丢弃。2026-07-04 本机执行该真实 app-bundle smoke 已触达 designed shell Start 按钮和真实 adapter path，但当前机器的 app-bundle Screen Recording/TCC 环境返回 `permission_denied`，UI 错误为 `Native capture permissions are denied or unknown. Error code: permission_denied`，因此该运行不能记为真实录制通过。后续补强 app-bundle window 等待后再次执行 `MA_NATIVE_APP_REAL_CAPTURE_SMOKE=1 ./platform/native-app/scripts/test-app-bundle.sh`，已越过窗口等待并点击 `ma.recording.startButton`，最终仍 fail closed 到同一 `permission_denied`；runner 输出 `real-capture-app-bundle-smoke.log`、app bundle 路径、TCC 设置入口和重跑命令。授予该测试 app bundle Screen Recording / Screen & System Audio Recording 权限后，2026-07-04 再次执行同一命令通过：1 个 app-bundle XCUITest 通过，UI 从 `Recording in progress.` 进入 `Recording saved.`，`Saved 1 recording artifact.`，`screen_video: available`，system/microphone/mixed audio 按 screen-only 策略为 `missing`。native lint/test/architecture gate 登记 capability、hook、script runner、window 诊断和 opt-in 边界 | 该证据把真实 ScreenCaptureKit 从脚本入口推进到 designed native shell 主操作触发的可显式运行 app-bundle smoke，并证明缺少本机 TCC 权限时 fail closed；授予本机 TCC 权限后已有一条 screen-only 真实 app-bundle 录制通过证据。状态仍保持 `partial`：不证明默认 app-bundle 测试覆盖真实 capture、不证明 Release 默认启用真实 adapter、不证明独立系统音频/麦克风音频产物、native-to-processing invocation、真实 file picker/pasteboard/delete、完整 release bundle 或任意 `PV-MA-*` covered |

## 2026-07-04 VS-MA-21 native capture stop retry 证据回填

本节补充真实/受控 native capture stop finalization 的异常与重试证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、UI state、Release hook 或发布范围；所有涉及 `PV-MA-*` 继续保持 `partial` 口径，不能声明真实 native capture、生产 app 默认真实录制、完整系统/麦克风音频、release bundle 或任意发布范围已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本地 `develop` after merge `a06c872` | `PV-MA-002`、`PV-MA-003`、`PV-MA-009`; `TDG-MA-002`、`TDG-MA-006`、`TDG-MA-007` | `NativeRecordingCommandClient` 在 `stopRecording` 中缓存已从 capture adapter 取得但尚未成功写入 `session.json` / artifact registry 的 stop finalization 结果；若首次 stop 因 `RecordingSessionStore` 的 `path_conflict` 等文件边界错误失败，下一次 stop 会复用第一次 adapter stop 得到的 artifact data、`ended_at` 和降级原因继续 finalize，不再次调用 capture adapter。`NativeRecordingCommandClientTests.stopFinalizationPathConflictCanRetryWithoutCallingAdapterAgain` 通过 artifact symlink 冲突制造首次 fail closed，移除冲突后重试成功写入 `screen_video` / `mixed_audio`，并断言 adapter stop 只调用一次、workspace 外目标未被覆盖、最终 `session.json` 为 `recorded` 且 4 类 artifact 登记完整 | 证明 native capture stop 的文件边界失败可解释、可重试，且不会通过重复 stop capture adapter 覆盖或丢失第一次 stop 取得的原始媒体证据；该证据只覆盖同一 app 进程内的受控 retry，不证明跨进程崩溃恢复、真实 ScreenCaptureKit 所有失败模式、独立音频产物、native-to-processing invocation 或 release coverage。相关行继续保持 `partial` |
| 本轮 `develop` VS-MA-21 restart recovery | `PV-MA-002`、`PV-MA-003`、`PV-MA-009`; `TDG-MA-002`、`TDG-MA-006`、`TDG-MA-007` | `RecordingSessionStore.existingSessionReference` 增加只读恢复入口，在不创建新 session 的前提下校验 session id、workspace/sessions/session/artifacts 目录边界、symlink 和 `session.json` 元数据；`NativeRecordingCommandClient.stopRecording` 在 `activeSessions` 丢失时可从受控 recovery workspace 恢复 reference。`NativeRecordingCommandClientTests.stopAfterClientRestartReturnsFinalArtifactsWithoutCallingAdapterAgain` 覆盖 app/client 重启后已 final 的 `recorded` session 直接返回持久化 artifact registry，且不调用新 adapter stop；`stopAfterClientRestartFailsClosedWhenAdapterLostRecordingState` 覆盖重启后仍为 `recording` 但 adapter 内存状态丢失时写入 `failed` session、4 类 failed artifact、可解释降级原因，并断言已存在媒体文件未被覆盖 | 证明 native capture stop 在 app/client 重启后能区分已持久化 final session 与 orphan recording session：前者幂等返回，后者 fail closed、可解释且不覆盖既有媒体文件。该证据仍不证明跨进程继续真实 ScreenCaptureKit 捕获、系统/麦克风独立音频、native-to-processing invocation、release bundle 或所有 macOS/TCC 失败模式；相关行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-MA-002`、`PV-MA-003`、`PV-MA-009`; `TDG-MA-002`、`TDG-MA-006`、`TDG-MA-007` | `NativeRecordingCommandClientTests.adapterArtifactTraversalPathFailsClosedOnStop`、`adapterArtifactAbsolutePathFailsClosedOnStop`、`adapterArtifactEmptyPathComponentFailsClosedOnStop`、`adapterArtifactUnsafeFormatFailsClosedOnStop`、`adapterArtifactCrossTypePathFailsClosedOnStop`、`sessionDirectorySymlinkFailsClosedBeforeAdapterStop`、`artifactDirectorySymlinkFailsClosedOnStop` 和 `duplicateAdapterArtifactTypeFailsClosedOnStop` 覆盖 native capture adapter 在 `stopRecording` 返回自定义 artifact `relativePath` / `format`、空路径段、跨 type path、同一 `artifact_type` 重复项，或 managed session/artifacts 目录被 symlink 替换时的边界：`artifacts/../outside-video.mov`、绝对路径、`artifacts//screen_video.mov`、`mp4/../../outside`、`screen_video` 写入 `mixed_audio.wav`、重复 `screen_video`、session directory symlink 和 artifacts directory symlink 均返回 `path_conflict`，adapter stop 只调用一次或在 session metadata 边界失败前不调用，不写入 session 外目标，不覆盖 workspace 外既有文件，不让不同 artifact registry entry 共用同一 managed file，也不 silently pick first 写入重复类型中的任一媒体。`./platform/native-app/scripts/test.sh` 已通过并执行 138 个 Swift Testing 用例和 12 个 hosted UI smoke | 证明受控 native capture stop finalization 不信任 adapter-supplied artifact path/format/type ownership/type cardinality 或 symlink 替换后的 managed session/artifact 目录，路径穿越、绝对路径、空路径段、unsafe filename format、跨 type path、重复 artifact type、session directory symlink 和 artifacts directory symlink 均 fail closed，补强 `session.json` / artifact registry 的 managed path 边界。状态仍保持 `partial`：不证明真实 ScreenCaptureKit 所有失败模式、独立系统/麦克风音频、native-to-processing invocation、release bundle 或所有发布范围 `PV-MA-*` covered |

## 2026-07-05 VS-MA-14/15 native capture smoke artifact validation 证据回填

本节补充 opt-in 真实 ScreenCaptureKit native capture smoke 对 stop artifact registration 的断言，不新增产品事实，不改变 command、artifact、event、error code、exit code、UI state、Release hook 或发布范围；相关 `PV-MA-*` 继续保持 `partial`。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` VS-MA-14/15 smoke validation | `PV-MA-002`、`PV-MA-003`、`PV-MA-009`; `TDG-MA-002`、`TDG-MA-005`、`TDG-MA-007` | `platform/native-app/scripts/native-capture-smoke.sh` 的生成 Swift smoke 新增对 `degradation_reason` 的结构化验证和输出：非 `available` artifact 必须带 `degradation_reason`；screen-only 运行时 `system_audio`、`microphone_audio`、`mixed_audio` 必须为 `missing` 且无 checksum；请求音频时相应 artifact 预期为 `degraded`。本轮命令 `MA_NATIVE_CAPTURE_SMOKE=1 MA_NATIVE_CAPTURE_SMOKE_WORKSPACE=/tmp/... MA_NATIVE_CAPTURE_SMOKE_BUILD_DIR=/tmp/... MA_NATIVE_CAPTURE_SMOKE_DURATION_SECONDS=1 ./platform/native-app/scripts/native-capture-smoke.sh` 通过，生成 Swift 编译成功，真实 ScreenCaptureKit 录制 `screen_video.mp4`，`screen_video` 为 `available`，大小 209010 bytes，checksum 为 `sha256:13fb46513b58da4c70fb3307a6cf6689184d80d77c00361deef2c2f0c3ad04ec`，三类音频 artifact 均为 `missing` 且输出 `artifact_degradation_reasons`，`validation_errors` 为空。`./platform/native-app/scripts/test.sh` 同步通过，执行 140 个 Swift Testing 用例和 12 个 hosted UI smoke | 证明 opt-in 真实 native capture smoke 已覆盖 `VS-MA-15` 的四类 artifact presence、screen-only 缺失音频 `degradation_reason`、managed path、非空 `screen_video` 和 checksum 断言；仍不证明所有开发机 macOS/TCC/display 环境可用、独立系统音频或麦克风音频产物、production app 默认真实 capture、native-to-processing invocation、release bundle 或任意发布范围 `PV-MA-*` covered |
| 本轮 `develop` VS-MA-14/15 native capture artifact E2E | `PV-MA-002`、`PV-MA-003`、`PV-MA-006`、`PV-MA-007`、`PV-MA-009`; `TDG-MA-002`、`TDG-MA-005`、`TDG-MA-007` | 新增 `platform/e2e/native-capture-artifact-smoke.sh`：显式设置 `MA_NATIVE_CAPTURE_SMOKE=1` 时先运行真实 ScreenCaptureKit `platform/native-app/scripts/native-capture-smoke.sh`，再用 `processing-cli` 的 workspace contract 读取同一 `session.json`，校验四类原始 artifact presence、`screen_video` managed relative path、非空文件和 checksum、三类音频缺失/降级 `degradation_reason`，随后调用公开 `generate_transcript --session-id` 并断言 screen-only 无可用音频时返回既有 `artifact_missing` / exit 3、无 traceback、且不登记 `normalized_audio`、`transcript_text` 或 `speaker_labels`。`platform/e2e/full-stack-smoke.sh` 已接入显式 opt-in stage：默认不设置 `MA_NATIVE_CAPTURE_SMOKE` 时输出 optional stage skipped；设置后在 provider smoke 与 native bridge smoke 之间运行真实 native capture artifact smoke。本轮命令 `MA_NATIVE_CAPTURE_SMOKE=1 MA_NATIVE_CAPTURE_SMOKE_DURATION_SECONDS=1 ./platform/e2e/native-capture-artifact-smoke.sh` 通过；`screen_video` 大小 249291 bytes，checksum 为 `sha256:b0801452863c50c998d404e702953f8be8ca188f881f847b9cef5e461042958f`，`validation_errors` 为空；未设置 opt-in 时同脚本以 exit 2 明确 skipped | 证明真实 native capture 的 stop artifact registry 可被 processing 侧 workspace contract 读取，并证明当前 screen-only 真实 capture 输出在没有可用音频时进入 processing fail-closed 路径且不污染派生产物；同时让 full-stack 入口具备显式纳入该真实 capture artifact stage 的能力。状态仍保持 `partial`：该 opt-in stage 默认不进入 release gate，不证明所有 macOS/TCC/display 环境、独立系统音频或麦克风音频产物、production app 默认真实 capture、native UI 真实触发 processing provider、完整 release bundle 或任意发布范围 `PV-MA-*` covered |
| 本轮 `develop` VS-MA-14/15 opt-in full-stack stabilization | `PV-MA-002`、`PV-MA-003`、`PV-MA-006`、`PV-MA-007`、`PV-MA-009`、`PV-MA-013`; `TDG-MA-005`、`TDG-MA-007`、`TDG-MA-008` | `platform/native-app/scripts/native-capture-smoke.sh` 新增 `MA_NATIVE_CAPTURE_SMOKE_TIMEOUT_SECONDS`，真实 `NativeCaptureSmoke` executable 超时会输出结构化 `timeout` summary 并返回 124；`AppleScreenCaptureKitNativeCaptureAdapter` 在 `startRecording` 中等待 `recordingOutputDidStartRecording`，stop 顺序调整为 remove recording output -> wait finish -> best-effort stop stream，并用受控 event state 确保 start/finish/failure/timeout 均恢复等待者，避免 Swift continuation leak。`platform/e2e/native-capture-artifact-smoke.sh` 默认最多 2 次尝试，失败时保留最后一次 summary，并用 `MA_NATIVE_CAPTURE_ARTIFACT_SMOKE_DISPLAY_WAKE_SECONDS` / `MA_NATIVE_CAPTURE_ARTIFACT_SMOKE_DISPLAY_WAKE_SETTLE_SECONDS` 启动 `caffeinate -d -u` display wake guard 降低 display asleep 导致的 no-display 误报。本轮 `./platform/e2e/full-stack-smoke.sh` 默认路径通过并明确跳过 opt-in stage；`MA_NATIVE_CAPTURE_SMOKE=1 MA_NATIVE_CAPTURE_SMOKE_DURATION_SECONDS=3 MA_NATIVE_CAPTURE_SMOKE_TIMEOUT_SECONDS=60 MA_NATIVE_CAPTURE_ARTIFACT_SMOKE_ATTEMPTS=2 ./platform/e2e/full-stack-smoke.sh` 通过并在 full-stack 顺序中运行真实 native capture artifact stage，summary `session-native-capture-smoke-20260704T171406Z-59084` 记录 `screen_video` 大小 1627764 bytes、checksum `sha256:7486c5d04ad90eca237da3bbd4456024b6a03ddb486a2a3139c26af248199b17`、三类音频 `missing` 且带 `artifact_degradation_reasons`，`validation_errors` 为空；`./platform/native-app/scripts/test.sh` 同步通过，执行 143 个 Swift Testing 用例和 12 个 hosted UI smoke | 证明 opt-in full-stack 入口已经能以有界方式纳入真实 native capture artifact stage，并能把 screen-only 真实录制产物交给 processing workspace contract 验证和 fail-closed transcript path。`PV-MA-002/003/006/007/009/013` 继续保持 `partial`：该证据仍是显式 opt-in、本机/TCC/display 依赖证据，不属于默认或 release-scope 标准门禁，不证明跨机器可重复性、独立系统音频或麦克风音频真实产物、production app 默认真实 capture、native UI 真实触发 processing provider、完整 release bundle 或任意发布范围 `PV-MA-*` covered |

## 2026-07-05 VS-MA-16 real processing CLI app-bundle smoke 证据回填

本节补充 native app-bundle 通过 UI 触发真实 `processing-cli` process runner 的 opt-in 证据入口，不新增产品事实，不改变 command、artifact、event、error code、exit code、UI state、Release hook 或发布范围；相关 `PV-MA-*` 继续保持 `partial`。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` VS-MA-16 real processing app-bundle route | `PV-MA-006`、`PV-MA-007`、`PV-MA-008`、`PV-MA-010`; `TDG-MA-004`、`TDG-MA-007`、`TDG-MA-008` | 新增 `AppBundleLocatorSmokeTests.testRealProcessingCLIProcessesNativeRecordingFromLaunchedAppBundleWhenExplicitlyEnabled` 和 `MA_NATIVE_APP_REAL_PROCESSING_SMOKE=1 ./platform/native-app/scripts/test-app-bundle.sh` 分支：测试在临时 workspace 中写入 `native_recording` + `mixed_audio.wav`，通过 `.app` UI 点击 `ma.processing.startButton`，让 `ProcessingCommandProcessRunner` 经 provider-owned `platform/e2e/ma-cli-local.sh` 调用真实 `processing-cli`，预期生成 `normalized_audio`、`transcript_text` 和 transcript-only `speaker_labels`，并在 UI 中显示 processing degraded/completed 状态。该分支 `build-for-testing` 已成功编译 app-bundle UI test，`./platform/native-app/scripts/architecture.sh` 已断言 provider wrapper 可执行且调用公开 `meeting_assistant_cli`。本机先前多次执行 `test-without-building`，包括默认 destination、清理用户态 `universalAccessAuthWarn` 后重跑、固定 `MA_NATIVE_APP_XCODE_DESTINATION='platform=macOS,arch=arm64'` 重跑、终止用户态 `ctkd -tw` 后直接复用 `.xctestrun` 重跑，以及 `caffeinate -d -u` display wake/settle 后重跑，均在测试 body 前被 macOS UI testing 初始化阻断，错误包括 `LocalAuthentication Code=-4 "System authentication is running"` / `The test runner failed to initialize for UI testing`；2026-07-05 07:43 固定 `platform=macOS,arch=arm64` 重跑时仍在测试 body 前失败，形态为 `Timed out while enabling automation mode`，脚本输出恢复步骤和 `testmanagerd` / runner / `loginwindow` 诊断摘录。2026-07-05 08:26 定位并清理另一个 worktree 残留的同 bundle id `MeetingAssistantNative` 进程后，同一命令 `MA_NATIVE_APP_XCODE_DESTINATION='platform=macOS,arch=arm64' MA_NATIVE_APP_REAL_PROCESSING_SMOKE=1 ./platform/native-app/scripts/test-app-bundle.sh` 通过：测试进入 body，点击 `ma.processing.startButton`，UI 显示 transcript-only processing 完成状态，workspace 中出现 `normalized_audio`、`transcript_text` 和 degraded `speaker_labels` artifact。runner log 写入 `platform/native-app/build/DerivedData/AppBundleUITests/real-processing-app-bundle-smoke.log`；脚本继续保留 Automation Mode blocker 的恢复提示和关键日志摘录，以便环境再次被同 bundle 残留进程或系统认证阻断时 fail closed | 证明 VS-MA-16 已有真实 processing-cli app-bundle UI smoke 路由、编译/架构检查和执行通过证据；`.app` UI 点击 -> `processing-cli` provider -> transcript/speaker artifact 链路已达本纵切退出口径。`PV-MA-006/007/008/010` 继续保持 `partial`：该证据仍是 opt-in app-bundle smoke，不等同于 release-scope full-stack 中真实 native capture、transcript review、copy/export/delete 和 release bundle 的同一条门禁链路 |

## 2026-07-05 VS-MA-17/18/19 real action CLI app-bundle route 证据回填

本节补充 native app-bundle 从真实 processing artifact 进入 transcript review、copy/export 和 delete action 的 opt-in 路由；不新增产品事实，不改变 command、artifact、event、error code、exit code、UI state、Release hook 或发布范围。2026-07-05 10:49 固定 `platform=macOS,arch=arm64` 重跑通过，证明 `VS-MA-17/18/19` 当前纵切已有执行通过证据；相关 `PV-MA-*` 继续保持 `partial`，因为该证据仍不是 release-scope 同链路门禁。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` VS-MA-17/18/19 real action app-bundle route | `PV-MA-010`、`PV-MA-011`、`PV-MA-012`; `TDG-MA-004`、`TDG-MA-006`、`TDG-MA-007`、`TDG-MA-008` | 新增 `AppBundleLocatorSmokeTests.testRealProcessingCLITranscriptReviewExportAndDeleteFromLaunchedAppBundleWhenExplicitlyEnabled` 和 `MA_NATIVE_APP_REAL_ACTION_SMOKE=1 ./platform/native-app/scripts/test-app-bundle.sh` 分支：测试先在 `.app` 中点击 `ma.processing.startButton`，经 `ProcessingCommandProcessRunner` 和 provider-owned `platform/e2e/ma-cli-local.sh` 生成真实 `transcript_text` 与 transcript-only `speaker_labels` artifact；随后重启 `.app` 读取同一 workspace/session，点击 `ma.transcriptAction.copyButton`、`exportButton` 和 `deleteConfirmButton`，让 `TranscriptActionProcessRunner` 经同一 provider-owned CLI 执行 `export_transcript` / `delete_session`，并断言导出文件保留、session root 被删除、`meeting_session.deleted.v1` event 不携带 transcript 文本。此前多次执行 `MA_NATIVE_APP_XCODE_DESTINATION='platform=macOS,arch=arm64' MA_NATIVE_APP_REAL_ACTION_SMOKE=1 ./platform/native-app/scripts/test-app-bundle.sh` 均 `build-for-testing` 成功但被当前本机 `testmanagerd` / XCTest Automation Mode / LocalAuthentication 阻断；2026-07-05 10:49 同一命令重跑通过，`build-for-testing` 与 `test-without-building` 均成功，1 个 XCUITest 执行通过、0 failure、耗时 85.893 秒。测试进入 body 后点击 `ma.processing.startButton`，UI 显示 `Processing completed with transcript-only speaker labels.`；重启同一 workspace/session 后断言 heading `Real processing CLI app-bundle fixture`、summary `Transcript has 1 segment for review.`、文本 `Fake transcript generated from local audio.` 和 `transcript-only fallback` 降级说明；copy 返回 `Copy complete.`，export 返回 `Export complete.` 并写入 workspace 外 Markdown 文件；delete prompt 包含 `session-app-ui-smoke` 和 `External exports are retained.`，confirm 后返回 `Delete complete.` / `Deleted session session-app-ui-smoke.` / `Retained 1 external export.` | 证明 VS-MA-17/18/19 已有 provider-owned real action app-bundle route、编译、架构和执行通过证据，当前纵切退出口径已满足。`PV-MA-010/011/012` 继续保持 `partial`：该证据仍是 opt-in app-bundle smoke，不证明真实 native capture、真实 runtime/model 质量、真实用户 Save Panel 交互、release bundle，或 `VS-MA-20` 同一条 release-scope full-stack 链路 |

## 2026-07-04 VS-MA-18/19 production action OS boundary 证据回填

本节补充 transcript copy/export 的生产 OS 边界证据。该回填同步 `12-ui-ux-design.md`，允许生产 app 在注入协议后使用 macOS pasteboard 和 `NSSavePanel`；不改变 `export_transcript` / `delete_session` command、artifact、event、error code、exit code、Release action process hook 或发布范围。`PV-MA-011` 继续保持 `partial`，不能声明 production action process bridge、真实 processing provider、真实 capture、delete OS 执行或 release bundle 已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` action OS boundary | `PV-MA-011`; `TDG-MA-004`、`TDG-MA-006` | `platform/native-app/Sources/MeetingAssistantNative/TranscriptActionOSClients.swift` 新增 `TranscriptActionPasteboardClipboard` 和 `TranscriptActionSavePanelDestinationSelector`，生产 app 非 XCTest 路径由 `MeetingAssistantNativeApp` 注入真实 macOS pasteboard 和 `NSSavePanel`，XCTest/app-bundle smoke 继续注入 memory clipboard 和 deterministic target，避免测试弹系统面板或污染真实 pasteboard。architecture guard 将 AppKit OS effect 限定在该文件，只允许 `NSPasteboard` 和 `NSSavePanel`，继续禁止 `NSOpenPanel`、直接 file mutation/delete、Process、provider internals、network API、secret marker 和自动下载；`TranscriptReviewActionsViewModelTests.savePanelDestinationSelectorSuggestsSafeFileNames` 覆盖保存文件名安全化；`./platform/native-app/scripts/lint.sh`、`architecture.sh` 和 `test.sh` 已通过，`test.sh` 执行 140 个 Swift Testing 用例和 12 个 hosted UI smoke | 证明 copy/export 的真实 OS clipboard/save-panel boundary 已从 TODO/禁止项推进到受控 production-injected 实现，并保留 XCTest deterministic boundary；仍不证明 production action command client 默认启用真实 `TranscriptActionProcessRunner`、真实导出命令在 release app 中执行、真实 processing provider、真实 capture、delete OS 执行、完整 release bundle 或 `PV-MA-011` covered。`PV-MA-011` 继续保持 `partial`，下一步需明确 release-scope action process bridge |

## 2026-07-04 VS-MA-20 production command-client default 证据回填

本节补充非 XCTest 原生 app 默认 command client 策略。该回填同步 `04-user-journeys-and-ui.md`、`08-implementation-guidance.md`、`12-ui-ux-design.md` 和 `11-adr.md`，明确 production processing/action command client 默认使用本地 process runner，Debug/XCTest fake 仅作为隔离 fixture。该回填不改变公开 command、artifact、event、error code、exit code、native recording hook 或发布范围；所有涉及 `PV-MA-*` 继续保持 `partial` 口径，不能声明真实 capture、真实 runtime/model、完整 release bundle 或 release gate 已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` production command-client default | `PV-MA-006`、`PV-MA-007`、`PV-MA-008`、`PV-MA-010`、`PV-MA-011`、`PV-MA-012`; `TDG-MA-004`、`TDG-MA-006`、`TDG-MA-007` | `MeetingAssistantNativeApp` 的 `NativeProcessingClientMode` 和 `NativeTranscriptActionClientMode` 现在在非 XCTest runtime 默认返回 `.process`，显式 `fake` env 只在 Debug/XCTest fixture 中生效；XCTest 默认仍为 deterministic fake，显式 `process` 继续可用于 app-bundle bridge 证据。`NativeControlPlaneSmokeTests` 的 source-contract 检查同步断言 processing/action 不再通过 Debug/XCTest-only process hook 才能使用 process runner，并继续保留 recording real-capture hook 的 Debug/XCTest/opt-in 边界。验证命令：`./platform/native-app/scripts/lint.sh`、`./platform/native-app/scripts/architecture.sh`、`./platform/native-app/scripts/test.sh`、`git diff --check`、`./scripts/docs-check.sh`、`./scripts/spec-sync-check.sh`、`./scripts/product-validation-check.py current-phase`、`./scripts/product-validation-check.py release`、`./scripts/agent-workflow-check.sh`、`./scripts/review-report.sh` | 移除“production/default processing 或 action command client 仍为 fake”这一阻塞口径，并证明 fake hook 与 XCTest fixture 隔离；相关 PV 仍保持 `partial`：该证据不证明真实 native capture、真实 `whisper.cpp` runtime 在 release bundle 中完整运行、真实 diarization quality、完整 release evidence、真实 macOS 分发包或所有发布范围 `PV-MA-*` covered。`PV-MA-006`/`007`/`008`/`010` 仍需 release-scope native processing/provider 到 transcript review 的完整门禁；`PV-MA-011`/`012` 仍需真实 action process runner、OS boundary 和 release bundle 组合证据 |

## 2026-07-05 VS-MA-14/15 production recording default 证据回填

本节补充非 XCTest 原生 app 默认 recording command client 策略。该回填同步 `04-user-journeys-and-ui.md`、`08-implementation-guidance.md`、`12-ui-ux-design.md` 和 `11-adr.md`，明确 production/default recording client 使用 Apple ScreenCaptureKit adapter，Debug/XCTest fake/controlled hook 仅作为隔离 fixture。该回填不改变公开 command、artifact、event、error code、exit code、workspace schema 或发布范围；`PV-MA-002` / `PV-MA-003` 继续保持 `partial`，不能声明真实 capture、stop artifact registry、release bundle 或 release gate 已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` production recording default | `PV-MA-002`、`PV-MA-003`; `TDG-MA-004`、`TDG-MA-005`、`TDG-MA-008` | `MeetingAssistantNativeApp` 的 `NativeRecordingClientMode` 现在在非 XCTest runtime 默认返回 `.appleScreenCaptureKit`，并组合 `NativeRecordingCommandClient`、`MacOSNativeCapturePermissionChecker` 和 `AppleScreenCaptureKitNativeCaptureAdapter`；`MA_NATIVE_RECORDING_CLIENT=fake` / `controlled` 只在 Debug/XCTest fixture 中生效，controlled 仍要求显式 recording workspace；Debug/XCTest 的 real capture app-bundle smoke 仍要求 `MA_NATIVE_CAPTURE_SMOKE=1` 和 opt-in workspace。`NativeControlPlaneSmokeTests` source-contract、`platform/native-app/scripts/architecture.sh` 和 `platform/native-app/tests/ArchitectureTest.md` 同步防止 production/default recording client 回退到 fake。 | 移除“production/default 录制策略未证明使用 Apple adapter”这一阻塞口径；相关 PV 仍保持 `partial`：该证据不证明 release-scope 标准门禁运行真实 ScreenCaptureKit、不证明跨机器 TCC/display 可重复、不证明独立系统音频/麦克风音频真实产物、不证明真实 native capture 到 processing/transcript/export/delete 的同链路成功、不证明 release bundle 或 VS-MA-23 release readiness。 |

## 2026-07-04 VS-MA-22 current security/supply-chain 门禁证据回填

本节补充当前阶段安全和供应链门禁对组件 `security`/`sbom` 命令的 fail-closed 证据。该回填不新增产品事实，不改变 dependency、runtime、model、agent policy、CI 权限、发布范围或 release 放行口径；所有涉及 `PV-MA-*`、`SEC-MA-*` 和 `PV-HARNESS-*` 继续保持既有状态，不能声明 release readiness 或任意产品行为已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `./scripts/supply-chain-check.sh current` 现在和 release 阶段一样执行 manifest 注册的组件 `sbom` gate，不再只做 action pin 与 manifest 校验；`scripts/tests/test_harnesslib.py::HarnessValidationTests.test_supply_chain_current_runs_registered_sbom_gates` 使用临时 repo fixture 和两个 fake component SBOM command 断言 current 阶段会实际调用每个注册组件的 `sbom` 命令。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_supply_chain_current_runs_registered_sbom_gates`、`./scripts/supply-chain-check.sh current`、`./scripts/security-check.sh`、`./scripts/agent-workflow-check.sh`、`./scripts/review-report.sh --require-evidence` 和 `./scripts/check.sh` | 证明 VS-MA-22 的 current 阶段供应链入口会执行组件 SBOM 证据，降低 current/check scope 漏跑 license/SBOM regression 的风险；该证据不证明完整 release provenance、签名、公证、真实 runtime/model license/hash/provenance sidecar 已满足，也不改变 `release-preflight.sh` 的发布候选条件。相关产品行继续保持 `partial`，release 仍需发布范围 `PV-MA-*` 全部 covered 后再进入 |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_security_check_runs_registered_security_gates` 使用临时 repo fixture 和两个 fake component security command 断言 current 阶段 `./scripts/security-check.sh` 会实际调用每个注册组件的 `security` 命令，并且成功时打印 `security-check passed.`。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_security_check_runs_registered_security_gates` 和 `./scripts/security-check.sh` | 证明 VS-MA-22 的 current 阶段安全入口会执行组件 security 证据，降低 current/check scope 漏跑 SAST/SCA/secret/container security regression 的风险；该证据仍不证明完整 SAST/SCA/license、真实 runtime/model license/hash/provenance sidecar、完整 release provenance、签名、公证或 release readiness。相关产品行继续保持 `partial` |
| 本轮 `develop` VS-MA-22 security report aggregation | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `platform/native-app/scripts/security.sh` 和 `platform/processing-cli/scripts/security.sh` 现在会生成 `security/security-report.json`；仓库级 `./scripts/security-check.sh` 在运行组件 security gate 前删除旧 report，之后聚合校验每个 production component 的 report 必须绑定 manifest component id，声明 `release_gate=validation-only`、SAST 静态扫描、SCA 依赖审查、secret scan、禁止网络/安装扫描、no-auto-download、未打包 runtime/model、无外部网络访问且 findings 为空。新增 `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_security_check_fails_when_production_component_security_report_is_missing` 和 `test_security_check_fails_when_production_component_security_report_has_unsafe_values`，断言组件 security 命令只退出 0 但不产出 report 或 report 字段退化时必须 fail closed；本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_security_check_runs_registered_security_gates scripts.tests.test_harnesslib.HarnessValidationTests.test_security_check_fails_when_production_component_security_report_is_missing scripts.tests.test_harnesslib.HarnessValidationTests.test_security_check_fails_when_production_component_security_report_has_unsafe_values scripts.tests.test_harnesslib.HarnessValidationTests.test_security_check_fails_when_registered_security_gate_fails` 和 `./scripts/security-check.sh` | 证明 current/check scope 的 security gate 不再只信任组件命令退出码，而要求所有 production component 产出机器可审查的 SAST/SCA/secret/no-auto-download 零 findings 报告，降低安全扫描假绿风险；该证据仍不证明完整 release-scope SAST/SCA、完整 license/provenance、签名、公证、真实 macOS app 分发包、真实 runtime/model sidecar 在所有机器可用，或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_supply_chain_current_fails_when_registered_sbom_gate_fails` 使用临时 repo fixture，让所有 manifest 注册组件的 `sbom` command 写入执行标记后以非零退出；随后运行 `./scripts/supply-chain-check.sh current` 并断言仓库级 current supply-chain 入口非零退出、传播 `harness runtime failed`，且不会打印 `supply-chain-check passed`。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_supply_chain_current_fails_when_registered_sbom_gate_fails` 和 `./scripts/supply-chain-check.sh current` | 证明 current 阶段供应链入口不会只检查 SBOM gate 是否被调用，而会在任一注册组件 SBOM 命令失败时 fail closed，避免失败的 license/SBOM 证据被误报为通过；该证据仍不证明完整 release provenance、签名、公证、真实 runtime/model license/hash/provenance sidecar、完整 SCA/license 覆盖或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_security_check_fails_when_registered_security_gate_fails` 使用临时 repo fixture，让所有 manifest 注册组件的 `security` command 写入执行标记后以非零退出；随后运行 `./scripts/security-check.sh` 并断言仓库级 security 入口非零退出、传播 `harness runtime failed`，且不会打印 `security-check passed`。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_security_check_fails_when_registered_security_gate_fails` 和 `./scripts/security-check.sh` | 证明 current 阶段 security 入口不会只检查 security gate 是否被注册，而会在任一注册组件 security 命令失败时 fail closed，避免失败的 SAST/SCA/secret/container security 证据被误报为通过；该证据仍不证明完整 SAST/SCA/license、真实 runtime/model license/hash/provenance sidecar、完整 release provenance、签名、公证或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_supply_chain_current_fails_when_production_component_lacks_sbom_gate` 使用临时 repo fixture，先给所有组件设置 fake SBOM 命令，再删除一个 production component 的 `commands.sbom`；随后运行 `./scripts/supply-chain-check.sh current` 并断言入口在 current manifest 校验阶段非零退出、报告 `commands.sbom must be a non-empty argv array`，且不会执行其他 SBOM command 或打印 passed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_supply_chain_current_fails_when_production_component_lacks_sbom_gate` 和 `./scripts/supply-chain-check.sh current` | 证明 current 阶段供应链入口不仅会执行已注册 SBOM gate，也会阻断 production component 缺失 SBOM 注册的假绿灯，避免未注册组件绕过 current/check scope 的供应链证据；该证据仍不证明完整 release provenance、签名、公证、真实 runtime/model license/hash/provenance sidecar、完整 SCA/license 覆盖或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_supply_chain_current_fails_when_production_component_has_non_argv_sbom_gate` 使用临时 repo fixture，先给所有组件设置 fake SBOM 命令，再把一个 production component 的 `commands.sbom` 改成 shell 字符串；随后运行 `./scripts/supply-chain-check.sh current` 并断言入口在 current manifest 校验阶段非零退出、报告 `commands.sbom must be a non-empty argv array`，且不会执行其他 SBOM command 或打印 passed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_supply_chain_current_fails_when_production_component_has_non_argv_sbom_gate` 和 `./scripts/supply-chain-check.sh current` | 证明 current 阶段供应链入口拒绝 shell 字符串形式的 SBOM gate 注册，保持 manifest 命令 argv 边界，避免 production component 通过不可审计的 shell 字符串绕过 current/check scope 的供应链证据；该证据仍不证明完整 release provenance、签名、公证、真实 runtime/model license/hash/provenance sidecar、完整 SCA/license 覆盖或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_supply_chain_current_fails_when_production_component_uses_shell_sbom_gate` 使用临时 repo fixture，先给所有组件设置 fake SBOM 命令，再把一个 production component 的 `commands.sbom` 改成 `["sh", "-c", ...]` shell 包装数组；随后运行 `./scripts/supply-chain-check.sh current` 并断言入口在 current manifest 校验阶段非零退出、报告 `commands.sbom must be a non-empty argv array`，且 shell marker 不会被写入、不会打印 passed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_supply_chain_current_fails_when_production_component_uses_shell_sbom_gate` 和 `./scripts/supply-chain-check.sh current` | 证明 current 阶段供应链入口不仅拒绝 shell 字符串，也拒绝 `sh -c` 数组包装的 SBOM gate 注册，避免 production component 通过可执行 shell 片段绕过 manifest argv 审计边界；该证据仍不证明完整 release provenance、签名、公证、真实 runtime/model license/hash/provenance sidecar、完整 SCA/license 覆盖或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_supply_chain_current_fails_when_production_component_uses_noop_sbom_gate` 使用临时 repo fixture，先给所有组件设置 fake SBOM 命令，再把一个 production component 的 `commands.sbom` 改成 `["true"]` no-op argv；随后运行 `./scripts/supply-chain-check.sh current` 并断言入口在 current manifest 校验阶段非零退出、报告 `commands.sbom must be a non-empty argv array`，且其他 fake SBOM marker 不会被写入、不会打印 passed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_supply_chain_current_fails_when_production_component_uses_noop_sbom_gate` 和 `./scripts/supply-chain-check.sh current` | 证明 current 阶段供应链入口拒绝把无实际证据产出的 no-op 命令注册为 production component 的 SBOM gate，避免组件用空操作制造 current/check scope 假绿灯；该证据仍不证明完整 release provenance、签名、公证、真实 runtime/model license/hash/provenance sidecar、完整 SCA/license 覆盖或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_security_check_fails_when_production_component_lacks_security_gate` 使用临时 repo fixture，先给所有组件设置 fake security 命令，再删除一个 production component 的 `commands.security`；随后运行 `./scripts/security-check.sh` 并断言入口在 current manifest 校验阶段非零退出、报告 `commands.security must be a non-empty argv array`，且 fake security marker 不会被写入、不会打印 passed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_security_check_fails_when_production_component_lacks_security_gate` 和 `./scripts/security-check.sh` | 证明 current 阶段 security 入口会阻断 production component 缺失 security gate 注册的假绿灯，避免未注册安全扫描绕过 current/check scope 的安全证据；该证据仍不证明完整 SAST/SCA/license、真实 runtime/model license/hash/provenance sidecar、完整 release provenance、签名、公证或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_security_check_fails_when_production_component_has_non_argv_security_gate` 使用临时 repo fixture，先给所有组件设置 fake security 命令，再把一个 production component 的 `commands.security` 改成 shell 字符串；随后运行 `./scripts/security-check.sh` 并断言入口在 current manifest 校验阶段非零退出、报告 `commands.security must be a non-empty argv array`，且 fake security marker 不会被写入、不会打印 passed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_security_check_fails_when_production_component_has_non_argv_security_gate` 和 `./scripts/security-check.sh` | 证明 current 阶段 security 入口拒绝 shell 字符串形式的 security gate 注册，保持 manifest 命令 argv 边界，避免 production component 通过不可审计的 shell 字符串绕过 current/check scope 的安全扫描证据；该证据仍不证明完整 SAST/SCA/license、真实 runtime/model license/hash/provenance sidecar、完整 release provenance、签名、公证或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_security_check_fails_when_production_component_uses_noop_security_gate` 使用临时 repo fixture，先给所有组件设置 fake security 命令，再把一个 production component 的 `commands.security` 改成 `["true"]` no-op argv；随后运行 `./scripts/security-check.sh` 并断言入口在 current manifest 校验阶段非零退出、报告 `commands.security must be a non-empty argv array`，且 fake security marker 不会被写入、不会打印 passed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_security_check_fails_when_production_component_uses_noop_security_gate` 和 `./scripts/security-check.sh` | 证明 current 阶段 security 入口拒绝把无实际安全扫描证据产出的 no-op 命令注册为 production component 的 security gate，避免组件用空操作制造 current/check scope 安全假绿灯；该证据仍不证明完整 SAST/SCA/license、真实 runtime/model license/hash/provenance sidecar、完整 release provenance、签名、公证或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-001`-`SEC-MA-005`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_security_check_fails_when_production_component_uses_shell_security_gate` 使用临时 repo fixture，先给所有组件设置 fake security 命令，再把一个 production component 的 `commands.security` 改成 `["sh", "-c", ...]` shell 包装数组；随后运行 `./scripts/security-check.sh` 并断言入口在 current manifest 校验阶段非零退出、报告 `commands.security must be a non-empty argv array`，且 shell marker 不会被写入、不会打印 passed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_security_check_fails_when_production_component_uses_shell_security_gate` 和 `./scripts/security-check.sh` | 证明 current 阶段 security 入口不仅拒绝 no-op，也拒绝 `sh -c` 数组包装的 security gate 注册，避免 production component 通过可执行 shell 片段绕过 manifest argv 审计边界或制造安全扫描假绿灯；该证据仍不证明完整 SAST/SCA/license、真实 runtime/model license/hash/provenance sidecar、完整 release provenance、签名、公证或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `platform/native-app/scripts/sbom.sh` 与 processing CLI 的组件 SBOM 门禁对齐，除 CycloneDX/name 外，新增 `specVersion=1.5`、`metadata.component.type=application`、稳定 `urn:uuid` serial、显式空第三方组件清单、release validation image 复制组件 SBOM、digest-pinned base image 和非 root `USER` 校验。本轮验证命令：`./platform/native-app/scripts/sbom.sh`、`./scripts/supply-chain-check.sh current`、`./scripts/security-check.sh`、`./scripts/agent-workflow-check.sh` 和 `./scripts/review-report.sh` | 证明 native app 已注册的组件 SBOM gate 不只检查文件存在和组件名称，而会 fail closed 于 SBOM 元数据或 release validation image 供应链边界退化；该证据不证明完整 license/SCA/provenance、签名、公证、真实 macOS app 分发包或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `platform/native-app/scripts/build.sh` 现在和 processing CLI build gate 一样校验 release validation Dockerfile 必须使用 digest-pinned base、非 root `USER`、复制组件元数据和 SBOM、只执行 validation-only `CMD`，并阻断 `curl`/`wget`/`brew install`/`pip install`/`npm install`；同时写出 `build/build-report.json`，明确 `release_gate_image=validation-only`、不打包 macOS app/runtime/model、无自动下载。本轮验证命令：`./platform/native-app/scripts/build.sh`、`./scripts/build.sh`、`./scripts/security-check.sh`、`./scripts/supply-chain-check.sh current`、`./scripts/agent-workflow-check.sh` 和 `./scripts/review-report.sh` | 证明 native app build gate 对 release validation image 的供应链边界有结构化证据和 fail-closed 检查；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA 或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `./scripts/build.sh` 在 manifest 注册组件 build gate 运行完成后，会聚合校验每个 production component 的 `build/build-report.json`，要求报告明确 `release_gate_image=validation-only`、digest-pinned base、非 root 用户、不打包 runtime/model、无自动下载、引用组件 SBOM，且 macOS app 不得被 release validation image 打包；`scripts/tests/test_harnesslib.py::HarnessValidationTests.test_build_gate_requires_production_component_build_reports` 和 `test_build_gate_fails_when_production_component_build_report_is_missing` 覆盖通过与缺失报告 fail-closed 路径。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_requires_production_component_build_reports scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_is_missing` 和 `./scripts/build.sh` | 证明仓库级 build gate 不再只信任组件脚本退出码，而会检查 release validation image 的结构化供应链证据是否由所有 production component 产生；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_is_invalid_json` 使用临时 repo fixture，让 production component build 命令写出存在但不是合法 JSON 的 `build/build-report.json`；随后运行 `./scripts/build.sh` 并断言仓库级 build evidence 聚合必须 fail closed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_is_invalid_json` 和 `./scripts/build.sh` | 证明仓库级 build gate 不会把不可解析的 release validation image 结构化证据当作合格 build evidence，避免组件脚本用损坏或非机器可读报告绕过供应链声明；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_is_not_json_object` 使用临时 repo fixture，让 production component build 命令写出合法 JSON array 而不是 JSON object 的 `build/build-report.json`；随后运行 `./scripts/build.sh` 并断言仓库级 build evidence 聚合必须 fail closed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_is_not_json_object` 和 `./scripts/build.sh` | 证明仓库级 build gate 要求 release validation image 证据是机器可读对象，不能用数组或其他非对象 JSON 绕过字段级供应链声明；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_has_unsafe_values` 使用临时 repo fixture，让所有 production component build 命令写出存在但不安全的 `build/build-report.json`：`packages_runtime_or_model=true` 且 `auto_downloads=true`；随后运行 `./scripts/build.sh` 并断言仓库级 build evidence 聚合必须 fail closed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_has_unsafe_values` 和 `./scripts/build.sh` | 证明仓库级 build gate 不仅要求 build report 存在，还会阻断组件脚本错误声明打包 runtime/model 或自动下载的退化；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_lacks_container_hardening` 使用临时 repo fixture，让 production component build 命令写出其他供应链字段合规、但 `digest_pinned_base=false` 且 `non_root_user=false` 的 `build/build-report.json`；随后运行 `./scripts/build.sh` 并断言仓库级 build evidence 聚合必须 fail closed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_lacks_container_hardening` 和 `./scripts/build.sh` | 证明 release validation image 结构化证据不能缺少 digest-pinned base 与非 root 用户硬化声明，避免把未满足容器供应链边界的 validation-only build 当作合格证据；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_misstates_identity_or_gate_image` 使用临时 repo fixture，让 production component build 命令写出其他供应链字段合规、但 `component` 与 manifest id 不一致且 `release_gate_image` 不是 `validation-only` 的 `build/build-report.json`；随后运行 `./scripts/build.sh` 并断言仓库级 build evidence 聚合必须 fail closed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_misstates_identity_or_gate_image` 和 `./scripts/build.sh` | 证明 release validation image 结构化证据必须绑定 manifest production component 身份，并保持 validation-only 口径，避免把错误组件或非 validation-only build 误当作当前组件供应链证据；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_packages_macos_app` 使用临时 repo fixture，让 production component build 命令写出其他供应链字段合规、但 `packages_macos_app=true` 的 `build/build-report.json`；随后运行 `./scripts/build.sh` 并断言仓库级 build evidence 聚合必须 fail closed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_packages_macos_app` 和 `./scripts/build.sh` | 证明 release validation image 证据不能被组件脚本误报成真实 macOS app 打包产物，避免把 validation-only build 混同为分发包、签名或公证证据；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_missing_sbom` 使用临时 repo fixture，让 production component build 命令写出其他供应链字段合规、但缺少 `sbom` 引用的 `build/build-report.json`；随后运行 `./scripts/build.sh` 并断言仓库级 build evidence 聚合必须 fail closed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_missing_sbom` 和 `./scripts/build.sh` | 证明仓库级 build gate 不允许 release validation image 结构化证据脱离组件 SBOM 证据，避免把缺少 SBOM 追溯的 validation-only build 当作供应链证据；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_misstates_sbom_name` 使用临时 repo fixture，让 production component build 命令写出其他供应链字段合规、但 `sbom` 引用不匹配组件目录下 CycloneDX SBOM 的 `metadata.component.name`；随后运行 `./scripts/build.sh` 并断言仓库级 build evidence 聚合必须 fail closed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_fails_when_production_component_build_report_misstates_sbom_name` 和 `./scripts/build.sh` | 证明仓库级 build gate 不只接受任意非空 SBOM 字符串，而会把 build report 绑定到组件实际生成的 SBOM 名称，避免组件脚本用错误或伪造的 SBOM 引用制造 release validation image 供应链假证据；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_build_gate_fails_when_production_component_sbom_is_invalid_json` 使用临时 repo fixture，让 production component build 命令写出其他供应链字段合规、且 `sbom` 引用预期组件名，但同时把组件目录下 CycloneDX SBOM 写成非法 JSON；随后运行 `./scripts/build.sh` 并断言仓库级 build evidence 聚合必须 fail closed。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_build_gate_fails_when_production_component_sbom_is_invalid_json` 和 `./scripts/build.sh` | 证明仓库级 build gate 不会把损坏或非机器可读的组件 SBOM 当作 release validation image 的供应链追溯证据，即使 build report 自身声明了正确 SBOM 名称也必须 fail closed；该证据仍不证明真实 macOS app 分发包、签名、公证、provenance、完整 license/SCA、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |

## 2026-07-05 VS-MA-21 concurrency retry checksum evidence 回填

本节补充 VS-MA-21 capture-style processing 并发锁冲突、重试和 checksum preservation 证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-*`、`SEC-MA-*` 和 `TDG-MA-*` 继续保持 `partial` 口径，不能声明 release-scope 真实 native capture 并发、真实 native UI、真实 release bundle 或 PV release readiness 已 covered。

| 来源 | 关联矩阵项 | 本轮证据 | 仍不证明 |
|---|---|---|---|
| Autopilot `codex/meeting-assistant-autopilot` | `PV-MA-006`、`PV-MA-007`、`PV-MA-009`; `TDG-MA-002`、`TDG-MA-006`、`TDG-MA-007` | `platform/e2e/capture-processing-smoke.sh` 的 `exercise_lock_conflict` 现在在同一 session lock 被其他执行上下文持有时，通过真实 CLI 子进程调用公开 `generate_transcript --session-id`，断言既有 `path_conflict` / exit 3、无 traceback、无 `normalized_audio` / `transcript_text` 登记、无 transcript 输出文件、无临时残留，并验证原始 capture artifact checksum 不变；释放 lock 后同一 session 立即重试 `generate_transcript --language zh` 必须成功，生成 `normalized_audio` 和 `transcript_text`，transcript source 指向 normalized artifact，原始 capture checksum 仍保持不变，并输出 `VS-MA-21 provider/e2e marker [non-contract]: capture-style concurrency/retry fixture - lock conflict exit 3 then retry success with capture checksums preserved verified`。本轮验证命令：`./platform/e2e/capture-processing-smoke.sh`、`./platform/processing-cli/scripts/test.sh`、`./scripts/spec-sync-check.sh`、`python3 scripts/product-validation-check.py current-phase`、`./scripts/agent-workflow-check.sh` 和 `./scripts/review-report.sh` | 证明当前 processing provider 侧 native_recording-style fixture 已覆盖一条进程级锁冲突后的失败不污染、释放后重试成功和 checksum preserved 路径；仍不证明真实 ScreenCaptureKit 并发、production/default 真实 native capture、native UI release-scope 并发、release bundle、完整 PV release readiness 或所有 macOS/TCC 环境。相关产品行继续保持 `partial` |

## 2026-07-05 VS-MA-21/22 full-stack marker attribution guard 回填

本节补充 full-stack smoke 证据归属的防误读回归。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-*`、`SEC-MA-*` 和 `TDG-MA-*` 继续保持 `partial` 口径，不能声明 VS-MA-23、release candidate、PV release readiness 或 release preflight 已进入。

| 来源 | 关联矩阵项 | 本轮证据 | 仍不证明 |
|---|---|---|---|
| 本轮 `develop` full-stack marker attribution guard | `PV-MA-006`、`PV-MA-007`、`PV-MA-009`、`PV-MA-013`; `TDG-MA-007`、`TDG-MA-008` | `platform/e2e/full-stack-smoke.sh` 结尾的非契约 summary 不再输出 `VS-MA-23 provider/e2e release readiness` marker，而是分别归属到 `VS-MA-20 provider/e2e attribution`、`VS-MA-21 provider/e2e hardening` 和 `VS-MA-21/22 provider/e2e blocker reminder`，并显式输出 `VS-MA-23 is not entered until partial PV rows close`。`platform/e2e/README.md` 同步说明 full-stack pass marker 不得写作 VS-MA-23 release readiness；`scripts/tests/test_harnesslib.py::HarnessValidationTests.test_full_stack_smoke_does_not_emit_premature_vs_ma_23_release_marker` 静态断言脚本不再包含过早 VS-MA-23 release marker。本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_full_stack_smoke_does_not_emit_premature_vs_ma_23_release_marker`、`bash -n platform/e2e/full-stack-smoke.sh`、`./scripts/spec-sync-check.sh`、`python3 scripts/product-validation-check.py current-phase`、`./scripts/agent-workflow-check.sh` 和 `./scripts/review-report.sh` | 只证明 provider/full-stack smoke 的证据标签不会把 VS-MA-20/21 当前阶段证据误标为 VS-MA-23 release readiness；仍不证明真实 ScreenCaptureKit 输出、真实用户 Save Panel、release distribution bundle、完整 SAST/SCA/license/provenance、release preflight、所有发布范围 `PV-MA-*` covered 或 VS-MA-23 已进入。相关产品行继续保持 `partial` |

## 2026-07-05 VS-MA-22 SBOM license and scope evidence 回填

本节补充 VS-MA-22 组件 SBOM license 与 scope fail-closed 证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-*`、`SEC-MA-*` 和 `TDG-MA-*` 继续保持 `partial` 口径，不能声明完整 release-scope SAST/SCA/license/provenance、签名、公证、真实 macOS app 分发包、model sidecar 或 release readiness 已 covered。

| 来源 | 关联矩阵项 | 本轮证据 | 仍不证明 |
|---|---|---|---|
| Autopilot `codex/meeting-assistant-autopilot` | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `platform/native-app/sbom/native-app.cdx.json` 和 `platform/processing-cli/sbom/processing-cli.cdx.json` 的 `metadata.component.licenses` 现在显式声明顶层仓库许可证 `Apache-2.0`，并通过 `metadata.properties` 固定 `meeting-assistant:license-evidence=Apache-2.0`、`meeting-assistant:packaged-third-party-runtime-components=none` 和 `meeting-assistant:release-gate-image=validation-only`；两个组件 `sbom.sh` 会在许可证、空第三方 runtime component 声明或 validation-only 范围声明缺失/错误时 fail closed。本轮验证命令：`./platform/native-app/scripts/sbom.sh`、`./platform/processing-cli/scripts/sbom.sh`、`./scripts/supply-chain-check.sh current`、`./scripts/security-check.sh`、`./scripts/spec-sync-check.sh`、`python3 scripts/product-validation-check.py current-phase`、`./scripts/agent-workflow-check.sh` 和 `./scripts/review-report.sh` | 证明当前 native app 与 processing CLI validation image 的组件 SBOM 不再只有空 `components`，而是具备可审计的 first-party license、无 packaged third-party runtime component 和 validation-only scope 声明，并且 current supply-chain 入口会执行这些组件 gate；仍不证明完整 SCA/license 扫描、release provenance、签名、公证、真实 macOS app 分发包、真实 runtime/model license/hash/provenance sidecar 或 release readiness。相关产品行继续保持 `partial` |

## 2026-07-05 VS-MA-22 model sidecar fail-closed evidence 回填

本节补充 VS-MA-22 release provider model sidecar fail-closed 证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-*`、`SEC-MA-*` 和 `TDG-MA-*` 继续保持 `partial` 口径，不能声明完整 release-scope SAST/SCA/license/provenance、签名、公证、真实 macOS app 分发包、model sidecar 或 release readiness 已 covered。

| 来源 | 关联矩阵项 | 本轮证据 | 仍不证明 |
|---|---|---|---|
| Autopilot `codex/meeting-assistant-autopilot` | `PV-MA-005`、`PV-MA-007`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `platform/processing-cli/scripts/test.sh` 现在用临时 `HOME` 构造 release provider fixture，除既有缺失 sidecar blocker 外，新增三条 fail-closed 负向证据：`MEETING_ASSISTANT_TRANSCRIPTION_MODEL_SHA256` 与实际模型 hash 不匹配时 `release-provider-smoke.sh` 必须报告 `configured sha256 does not match the selected model`；JSON provenance sidecar 损坏时必须报告 `JSON sidecar cannot be parsed`；显式 provenance sidecar 位于模型根目录外时必须报告 `model provenance sidecar blocker`。这些断言与 `./scripts/release-provider-smoke.sh` 的本机 `.local` runtime/model/audio root、license/provenance sidecar 和 no-auto-download 规则一起进入 processing CLI 组件测试。本轮验证命令：`./platform/processing-cli/scripts/test.sh`、`./scripts/security-check.sh`、`./scripts/supply-chain-check.sh current`、`./scripts/spec-sync-check.sh`、`python3 scripts/product-validation-check.py current-phase`、`./scripts/agent-workflow-check.sh` 和 `./scripts/review-report.sh` | 证明 release provider smoke 不会把 hash mismatch、不可解析 provenance 或模型根目录外的 sidecar 当作合格模型来源证据；仍不证明完整 SCA/license 扫描、release provenance、签名、公证、真实 macOS app 分发包、真实 runtime/model sidecar 在所有开发机可用，或 release readiness。相关产品行继续保持 `partial` |

## 2026-07-05 VS-MA-22 release-provider path boundary evidence 回填

本节补充 VS-MA-22 release-provider 本机共享资产路径边界和 marker 归属证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-*`、`SEC-MA-*` 和 `TDG-MA-*` 继续保持 `partial` 口径，不能声明完整 release-scope SAST/SCA/license/provenance、签名、公证、真实 macOS app 分发包、model sidecar 或 release readiness 已 covered。

| 来源 | 关联矩阵项 | 本轮证据 | 仍不证明 |
|---|---|---|---|
| 本轮 `develop` VS-MA-22 release-provider path boundary hardening | `PV-MA-005`、`PV-MA-007`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `platform/processing-cli/scripts/release-provider-smoke.sh` 的非契约 marker 从 `VS-MA-23 provider release smoke` 调整为 `VS-MA-22 release-provider smoke`，避免把 provider 安全证据误读为 release candidate 放行证据；`platform/processing-cli/scripts/test.sh` 在临时 `HOME` 的合规 sidecar fixture 基础上新增四条 fail-closed 断言：runtime 可执行文件位于 `.local` 允许根外时必须报告 `runtime path blocker`，模型文件位于 `~/.local/share/ai-models/whisper.cpp/` 外时必须报告 `model path blocker`，English-only `.en` 模型必须报告 `English-only .en model` blocker，mixed-language audio fixture 位于 `~/.local/share/ai-fixtures/asr/zh-en-tech/` 外时必须报告 `audio fixture path blocker`。本轮验证命令：`bash -n platform/processing-cli/scripts/release-provider-smoke.sh platform/processing-cli/scripts/test.sh`、`./platform/processing-cli/scripts/test.sh`、`./scripts/spec-sync-check.sh`、`python3 scripts/product-validation-check.py current-phase`、`./scripts/agent-workflow-check.sh` 和 `./scripts/review-report.sh` | 证明 VS-MA-22 release-provider smoke 不再以 VS-MA-23 marker 呈现，并且组件测试会阻断 runtime/model/audio 从仓库、临时目录或其他非允许根目录取证，也会阻断 English-only 模型冒充 mixed-language evidence；仍不证明完整 release-scope SAST/SCA/license/provenance、真实 runtime/model sidecar 在所有开发机可用、真实 macOS app 分发包、签名、公证、release bundle 或 release readiness。相关产品行继续保持 `partial` |

## 2026-07-05 VS-MA-22 supply-chain report aggregation evidence 回填

本节补充 VS-MA-22 current/check scope 的供应链结构化报告聚合证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-*`、`SEC-MA-*` 和 `TDG-MA-*` 继续保持 `partial` 口径，不能声明完整 release-scope SCA/license/provenance、签名、公证、真实 macOS app 分发包、model sidecar 或 release readiness 已 covered。

| 来源 | 关联矩阵项 | 本轮证据 | 仍不证明 |
|---|---|---|---|
| 本轮 `develop` VS-MA-22 supply-chain report aggregation | `PV-HARNESS-006`; `SEC-MA-004`; `TDG-MA-006`、`TDG-MA-007` | `platform/native-app/scripts/sbom.sh` 和 `platform/processing-cli/scripts/sbom.sh` 现在会生成 `supply-chain/supply-chain-report.json`；仓库级 `./scripts/supply-chain-check.sh current` 在运行组件 SBOM gate 前删除旧 report，之后聚合校验每个 production component 的 report 必须绑定 manifest component id，声明 `release_gate=validation-only`、CycloneDX JSON SBOM、first-party `Apache-2.0` license、SCA dependency review、license review、无 packaged third-party runtime component、不打包 runtime/model、不自动下载、validation-only provenance scope、未生成 release provenance attestation 且 findings 为空。新增 `scripts/tests/test_harnesslib.py::HarnessValidationTests.test_supply_chain_current_fails_when_production_component_supply_chain_report_is_missing` 和 `test_supply_chain_current_fails_when_production_component_supply_chain_report_has_unsafe_values`，断言组件 SBOM 命令只退出 0 但不产出 report 或 report 字段退化时必须 fail closed；本轮验证命令：`python3 -m unittest scripts.tests.test_harnesslib.HarnessValidationTests.test_supply_chain_current_runs_registered_sbom_gates scripts.tests.test_harnesslib.HarnessValidationTests.test_supply_chain_current_fails_when_production_component_supply_chain_report_is_missing scripts.tests.test_harnesslib.HarnessValidationTests.test_supply_chain_current_fails_when_production_component_supply_chain_report_has_unsafe_values scripts.tests.test_harnesslib.HarnessValidationTests.test_supply_chain_current_fails_when_registered_sbom_gate_fails` 和 `./scripts/supply-chain-check.sh current` | 证明 current/check scope 的 supply-chain gate 不再只信任组件 SBOM 命令退出码，而要求所有 production component 产出机器可审查的 SBOM/license/SCA/validation-only provenance/零 findings 报告，降低供应链假绿风险；该证据仍不证明完整 release-scope SCA/license/provenance、签名、公证、真实 macOS app 分发包、真实 runtime/model sidecar 在所有机器可用，或 release readiness。相关产品行继续保持 `partial` |

## 2026-07-05 VS-MA-22 real runtime app-bundle smoke 入口与阻断记录

本节记录 native app-bundle 到真实 `whisper.cpp` runtime 的 opt-in smoke 入口和当前本机执行阻断。该记录不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-*`、`SEC-MA-*` 和 `TDG-MA-*` 继续保持 `partial` 口径，不能声明真实 runtime app-bundle、release bundle 或 release readiness 已 covered。

| 来源 | 关联矩阵项 | 本轮证据 | 仍不证明 |
|---|---|---|---|
| 本轮 `develop` VS-MA-22 real runtime app-bundle smoke 入口 | `PV-MA-007`、`PV-MA-010`; `SEC-MA-004`; `TDG-MA-004`、`TDG-MA-006`、`TDG-MA-007` | 新增 `AppBundleLocatorSmokeTests.testRealWhisperRuntimeTranscriptReviewFromLaunchedAppBundleWhenExplicitlyEnabled` 和 `MA_NATIVE_APP_REAL_RUNTIME_SMOKE=1 ./platform/native-app/scripts/test-app-bundle.sh` 分支。测试入口在临时 workspace 中写入 `native_recording` session 和从 `MEETING_ASSISTANT_WHISPER_SMOKE_AUDIO` 复制的 `mixed_audio` fixture，通过 launched `.app` 点击 `ma.processing.startButton`，让 `ProcessingCommandProcessRunner` 经 provider-owned `platform/e2e/ma-cli-local.sh` 调用真实 `generate_transcript --runtime whisper_cpp --language zh`，预期生成真实 runtime transcript 后再由 transcript review 读取同一 workspace/session；入口只在 Debug/XCTest 显式 `MA_NATIVE_APP_REAL_RUNTIME_SMOKE=1` 时启用，不下载、不复制共享模型、不打包 runtime/model/audio。2026-07-05 固定 `MA_NATIVE_APP_XCODE_DESTINATION='platform=macOS,arch=arm64'` 并显式传入本机 `MEETING_ASSISTANT_TRANSCRIPTION_RUNTIME`、`MEETING_ASSISTANT_TRANSCRIPTION_MODEL` 和 `MEETING_ASSISTANT_WHISPER_SMOKE_AUDIO` 后，`build-for-testing` 已通过；但 `test-without-building` 两次在 test body 前被 macOS UI automation 初始化阻断，错误为 `Timed out while enabling automation mode`。尝试终止当前用户态 `testmanagerd` 后重跑仍显示 `_XCT_enableAutomationModeWithReply:` transaction 残留与 `active count = 5`，因此该运行只能作为本机 Automation Mode 环境 blocker，不能作为真实 runtime app-bundle 通过证据。 | 不能证明 `.app` 已成功触发真实 `whisper.cpp` runtime、不能证明 transcript review 读回真实 runtime transcript、不能证明真实 ScreenCaptureKit 输出、真实用户 Save Panel、release distribution bundle、所有开发机 runtime/model 可用、完整 SAST/SCA/license/provenance 或 release readiness。`PV-MA-007` / `PV-MA-010` 和相关安全/技术债行继续保持 `partial`；解除本机 UI automation blocker 后必须重跑同一 smoke，且通过后仍只能作为 opt-in app-bundle partial evidence |

## 2026-07-05 VS-MA-22 real runtime native bridge smoke 证据回填

本节补充 VS-MA-22 不依赖 app-bundle UI automation 的 native real runtime bridge smoke 证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-*`、`SEC-MA-*` 和 `TDG-MA-*` 继续保持 `partial` 口径，不能声明真实 app-bundle、release bundle 或 release readiness 已 covered。

| 来源 | 关联矩阵项 | 本轮证据 | 仍不证明 |
|---|---|---|---|
| 本轮 `develop` VS-MA-22 real runtime native bridge smoke | `PV-MA-007`、`PV-MA-010`; `SEC-MA-004`; `TDG-MA-004`、`TDG-MA-006`、`TDG-MA-007` | 新增 `ProcessingStateViewModelTests.processRunnerWithRealWhisperRuntimeWritesWorkspaceArtifactsLoadableByTranscriptReviewWhenEnabled`，并在 `platform/native-app/scripts/test.sh` 中加入显式 `MA_NATIVE_REAL_RUNTIME_BRIDGE_SMOKE=1` 分支。该测试在临时 workspace 写入 `native_recording` session，将 `MEETING_ASSISTANT_WHISPER_SMOKE_AUDIO` 复制为 `mixed_audio`，从 `ProcessingStateViewModel.start(sessionID:language:runtime:)` 传入 `language=zh` / `runtime=whisper_cpp`，经 `ProcessingCommandProcessRunner` 调用 provider-owned CLI 和用户配置的 `whisper.cpp` runtime/model，断言 `mixed_audio`、`normalized_audio`、`transcript_text`、`speaker_labels` artifact 登记、源音频 checksum 不变、真实 transcript 含中文和 `http` / `llm` / `clean architecture` / `eda`，并由 `TranscriptReviewWorkspaceLoader` 读回 transcript-only fallback。2026-07-05 显式传入本机 `MEETING_ASSISTANT_TRANSCRIPTION_RUNTIME`、`MEETING_ASSISTANT_TRANSCRIPTION_MODEL` 和 `MEETING_ASSISTANT_WHISPER_SMOKE_AUDIO` 后，`MA_NATIVE_REAL_RUNTIME_BRIDGE_SMOKE=1 ./platform/native-app/scripts/test.sh` 通过，输出 marker `ProcessingCommandProcessRunner -> provider CLI -> whisper.cpp -> TranscriptReviewWorkspaceLoader verified`。 | 不能证明 `.app` UI 点击、app-bundle XCUITest、真实 ScreenCaptureKit 输出、真实用户 Save Panel、release distribution bundle、完整 SAST/SCA/license/provenance、所有开发机 runtime/model/audio 可用，或 release readiness。`PV-MA-007` / `PV-MA-010` 和相关安全/技术债行继续保持 `partial`；解除本机 UI automation blocker 后仍必须重跑 real runtime app-bundle smoke |

## 2026-07-05 VS-MA-20 native app-bundle MVP full-stack smoke 证据回填

本节补充 VS-MA-20 同一 workspace/session 的 app-bundle MVP full-stack smoke 证据。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-*` 继续保持 `partial` 口径，不能声明真实 ScreenCaptureKit 输出、真实用户 Save Panel、release bundle、完整 release provenance 或 release gate 已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` VS-MA-20 MVP full-stack app-bundle smoke | `PV-MA-002`、`PV-MA-003`、`PV-MA-006`、`PV-MA-007`、`PV-MA-008`、`PV-MA-009`、`PV-MA-010`、`PV-MA-011`、`PV-MA-012`、`PV-MA-013`; `TDG-MA-003`、`TDG-MA-004`、`TDG-MA-005`、`TDG-MA-006`、`TDG-MA-007`、`TDG-MA-008` | 新增 `AppBundleLocatorSmokeTests.testMVPFullStackDesignedShellRecordingProcessingTranscriptActionsWhenExplicitlyEnabled` 和 `MA_NATIVE_APP_MVP_FULL_STACK_SMOKE=1 ./platform/native-app/scripts/test-app-bundle.sh` 分支；`platform/e2e/full-stack-smoke.sh` 可在显式 `MA_NATIVE_APP_MVP_FULL_STACK_SMOKE=1` 时追加该 app-bundle stage。该测试从 launched `.app` designed shell 断言 `ma.shell.*` locator，点击 `ma.recording.startButton` / `ma.recording.stopButton`，通过 Debug/XCTest-only controlled recording fixture 在同一 workspace/session 登记 `screen_video` 和真实 WAV `mixed_audio`；随后点击 `ma.processing.startButton`，经 provider-owned `platform/e2e/ma-cli-local.sh` 调用真实 `processing-cli`，生成 `normalized_audio`、`transcript_text` 和 transcript-only `speaker_labels`；测试重启 `.app` 并用 `MA_NATIVE_TRANSCRIPT_WORKSPACE` / `MA_NATIVE_TRANSCRIPT_SESSION_ID` 读取同一 session，断言 transcript heading、summary、文本和降级说明可见，再点击 copy/export/delete，验证导出 Markdown 文件写入 workspace 外、session root 被删除、外部导出保留、`meeting_session.deleted.v1` event 不携带 transcript 文本。2026-07-05 执行 `MA_NATIVE_APP_XCODE_DESTINATION='platform=macOS,arch=arm64' MA_NATIVE_APP_MVP_FULL_STACK_SMOKE=1 ./platform/native-app/scripts/test-app-bundle.sh` 通过：1 个 app-bundle XCUITest 通过、0 failure、耗时 173.663 秒，脚本输出 `native-app MVP full-stack app-bundle XCUITest passed.` | 证明 VS-MA-20 已有一条 opt-in app-bundle MVP full-stack 证据，串起 designed shell、受控 native recording、provider-owned CLI processing、transcript review、copy/export 和 delete confirmation，满足当前阶段退出口径。相关 PV 仍保持 `partial`：该证据显式 opt-in、依赖 Xcode UI automation，recording 是 controlled fixture 而不是真实 ScreenCaptureKit 输出，不证明独立系统音频/麦克风音频真实产物、真实用户 Save Panel、真实 release bundle、完整 SAST/SCA/license/provenance、真实 runtime/model 在发布包内可用，或所有发布范围 `PV-MA-*` covered；`./scripts/product-validation-check.py release` 仍应失败，进入 `VS-MA-23` 前必须先完成 `VS-MA-21/22` 与 release-scope 关闭条件 |

## 2026-07-05 VS-MA-14/15 phase-preflight opt-in evidence 口径回填

本节补充 VS-MA-14/15 真实 native capture artifact smoke 在阶段收口入口中的显式 opt-in 口径。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-002` / `PV-MA-003` 继续保持 `partial`，不能声明真实 capture、stop artifact registry、release bundle 或 release readiness 已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` VS-MA-14/15 phase-preflight opt-in marker | `PV-MA-002`、`PV-MA-003`; `TDG-MA-004`、`TDG-MA-008` | `scripts/phase-preflight.sh` 在显式 `MA_NATIVE_CAPTURE_SMOKE=1` 时会输出 `VS-MA-14/15 real native capture artifact smoke will run inside full-stack E2E; this remains partial evidence, not release readiness.`，并继续通过 `./scripts/test-e2e-full-stack.sh` 调用 manifest full-stack E2E。`scripts/tests/test_harnesslib.py` 新增静态回归，断言 phase-preflight 仍包含该 opt-in marker、仍调用 full-stack E2E，`full-stack-smoke.sh` 仍将 `MA_NATIVE_CAPTURE_SMOKE=1` 接到 `native-capture-artifact-smoke.sh`，且 `docs/engineering/02-dev-commands.md` 记录 `MA_NATIVE_CAPTURE_SMOKE=1 ./scripts/phase-preflight.sh` 的 partial/not-release 口径。 | 证明阶段收口日志可以明确显示 VS-MA-14/15 真实 capture artifact opt-in stage 被请求，并且该 stage 仍只能作为 partial evidence；不证明默认门禁运行真实 ScreenCaptureKit、不证明 release-scope standard gate、独立系统音频/麦克风音频产物、跨机器 TCC/display 可重复、production/default 录制策略、真实 native-to-processing 成功链路或 VS-MA-23 release readiness。 |

## 2026-07-05 VS-MA-14/15 native capture structured report evidence 回填

本节补充 VS-MA-14/15 真实 native capture artifact smoke 的结构化 evidence report。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-002` / `PV-MA-003` 继续保持 `partial`，不能声明真实 capture、stop artifact registry、release bundle 或 release readiness 已 covered。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` VS-MA-14/15 native capture artifact report | `PV-MA-002`、`PV-MA-003`; `TDG-MA-005`、`TDG-MA-007`、`TDG-MA-008` | 新增 `platform/e2e/native_capture_artifact_report.py`，`platform/e2e/native-capture-artifact-smoke.sh` 在真实 ScreenCaptureKit native smoke 成功后会调用它生成 JSON evidence report。report 声明 `report_schema=1`、`release_gate=partial-evidence-only`、`not_release_readiness=true`，并记录 `VS-MA-14/15`、`PV-MA-002/003`、workspace/session、adapter、四类原始 artifact 状态、`screen_video` bytes/checksum 校验、三类音频缺失/降级原因、`generate_transcript` 在无可用音频时以 `artifact_missing` / exit 3 fail closed、以及无 `normalized_audio` / `transcript_text` / `speaker_labels` 派生产物污染。新增 `scripts/tests/test_native_capture_artifact_report.py` 使用 synthetic native session 覆盖 report 写入、缺失音频降级原因 fail closed、CLI marker 和 report 路径输出。 | 证明 VS-MA-14/15 opt-in 真实 native capture smoke 的结果可以形成机器可审查 report，降低只靠 stdout marker 的假绿风险；仍不证明默认门禁运行真实 ScreenCaptureKit、不证明 release-scope standard gate、独立系统音频/麦克风音频真实产物、跨机器 TCC/display 可重复、production/default 录制策略、真实 native-to-processing 成功链路、release bundle 或 VS-MA-23 release readiness。 |

## 2026-07-05 VS-MA-14 native capture failure diagnostics report 回填

本节补充 VS-MA-14 真实 native capture 失败路径的结构化诊断 report。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；所有涉及 `PV-MA-002` / `PV-MA-003` 继续保持 `partial`，失败 report 不能作为通过证据。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` VS-MA-14 native capture failure diagnostics report | `PV-MA-002`、`PV-MA-003`; `TDG-MA-005`、`TDG-MA-007`、`TDG-MA-008` | `platform/e2e/native-capture-artifact-smoke.sh` 在真实 ScreenCaptureKit native smoke 失败时，会复制最后一次 native summary，并调用 `platform/e2e/native_capture_artifact_report.py --failure-report` 生成结构化 failure report；report 记录 `failure_stage`、native exit code、attempt count、display wake guard、start/stop response、validation errors、notes、TCC/display/runtime/timeout/artifact validation 诊断布尔值和 remediation hints，然后脚本仍保留 native smoke 原始失败退出码。新增 `scripts/tests/test_native_capture_artifact_report.py` 覆盖 permission/TCC 类失败 summary 的 report 写入、CLI marker 和 shell 失败分支静态接线。 | 证明失败的真实 native capture 运行能留下可比较的跨机器诊断证据，而不是只靠 stderr；仍不证明真实 capture 已通过、默认门禁运行 ScreenCaptureKit、release-scope standard gate、跨机器 TCC/display 可重复已关闭、独立音频产物、production/default 录制策略、真实 native-to-processing 成功链路或 release readiness。 |

## 2026-07-05 VS-MA-14/15 release-scope native capture gate 回填

本节补充 VS-MA-14/15 release-scope native capture artifact gate 的标准入口。该回填不新增产品事实，不改变 command、artifact、event、error code、exit code、Release hook、真实 capture 策略或发布范围；`PV-MA-002` / `PV-MA-003` 继续保持 `partial`，因为该 gate 仍不证明独立音频产物、跨机器可重复性、完整 native-to-processing 成功 transcript 链或 release bundle。

| 来源 | 影响矩阵行 | 追加证据 | 状态影响 |
|---|---|---|---|
| 本轮 `develop` VS-MA-14/15 release-scope native capture gate | `PV-MA-002`、`PV-MA-003`; `TDG-MA-005`、`TDG-MA-007`、`TDG-MA-008` | 新增 `platform/e2e/release-native-capture-artifact-smoke.sh`，固定设置 `MA_NATIVE_CAPTURE_SMOKE=1` 和 `MA_NATIVE_CAPTURE_RELEASE_SCOPE=1` 后调用同一个真实 ScreenCaptureKit artifact smoke；`scripts/release-preflight.sh` 在 `product-validation-check.py release` 之后注册该 gate，因此当前 PV 仍有 `partial` 时会先 fail closed。`platform/e2e/native_capture_artifact_report.py` 新增 `release_scope_native_capture=true` 与 `release_gate=release-scope-native-capture` report 口径，并继续写入 `not_release_readiness=true` 和 residual blockers。本轮命令 `MA_NATIVE_CAPTURE_ARTIFACT_SMOKE_ATTEMPTS=1 MA_NATIVE_CAPTURE_SMOKE_DURATION_SECONDS=2 ./platform/e2e/release-native-capture-artifact-smoke.sh` 通过，report `platform/e2e/build/native-capture-artifact-smoke/reports/native-capture-report-20260705T111547Z-5460.json` 记录 session `session-native-capture-smoke-20260705T111550Z-5529`、`screen_video` available、大小 4380902 bytes、checksum verified、三类音频 missing 且带 degradation reason，processing `generate_transcript` 返回既有 `artifact_missing` / exit 3 且无 `normalized_audio` / `transcript_text` / `speaker_labels` 污染。 | 证明 VS-MA-14/15 已有 release-scope native capture/stop artifact 标准门禁入口，并在本机受控 TCC/display 环境通过；`VS-MA-14/15` 可按阶段退出口径收口。`PV-MA-002/003` 仍保持 `partial`：还不证明独立系统/麦克风音频真实产物、跨机器 TCC/display 可重复、真实 native capture 到 processing 成功 transcript/review/export/delete 的同链路、release distribution bundle 或 VS-MA-23 release readiness。 |

## 标准验证命令目标

```bash
./scripts/docs-check.sh
./scripts/adoption-check.sh
./scripts/project-manifest-check.sh
./scripts/harness-self-test.sh
./scripts/compose-check.sh
./scripts/prod-config-check.sh
./scripts/architecture-check.sh
./scripts/security-check.sh
./scripts/supply-chain-check.sh
./scripts/db-migration-check.sh
./scripts/spec-sync-check.sh
./scripts/agent-workflow-check.sh
./scripts/check.sh
./scripts/test-e2e.sh
./scripts/test-e2e-full-stack.sh
./scripts/build.sh
./scripts/release-preflight.sh
```

## 产品级完成标准

一个产品行为只有同时满足以下条件，才能视为完成：

1. 对应事实源已更新或确认不需要更新。
2. 对应矩阵行存在。
3. 关键路径有自动化验证，状态达到 `covered`。
4. 如果短期只能使用 `manual-evidence`，交付说明必须写明证据、缺口和下一步测试落点。
5. 验证不依赖宿主机数据库、中间件或本机常驻服务。
6. 对应 `TDG-MA-*` 已关闭，且验证入口被标准组件命令、`./scripts/test.sh`、`./scripts/check.sh` 或 release-scope 门禁调用。

# 前端工作区

前端应用放在 `frontend/apps/` 下，共享包放在 `frontend/packages/` 下。

推荐的第一个应用：

```text
frontend/apps/web/
├── package.json
├── Dockerfile
├── src/
└── tests/
```

创建骨架前，先阅读 `docs/engineering/01-repo-structure.md` 和 `docs/product-spec/08-implementation-guidance.md`。

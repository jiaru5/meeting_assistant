services:
  postgres:
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-app_test}
      POSTGRES_USER: ${POSTGRES_USER:-app_test}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-app_test_password}
    volumes: !reset []
    tmpfs:
      - /var/lib/postgresql/data

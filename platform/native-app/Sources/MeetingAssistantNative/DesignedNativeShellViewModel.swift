/// Compatibility identifiers retained from the completed Phase 1 shell.
/// MVP.1 task state lives in `MeetingWorkspaceCoordinator`; the removed
/// six-section pipeline model must not be reintroduced here.
public enum DesignedNativeShellAccessibilityID {
    public static let root = "ma.shell.root"
    public static let subtitle = "ma.shell.subtitle"
}

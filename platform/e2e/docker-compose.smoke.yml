services:
  meeting-assistant-smoke:
    image: ${MEETING_ASSISTANT_SMOKE_IMAGE:-meeting-assistant-smoke:local}
    pull_policy: never
    command: ["sh", "-c", "sleep 3600"]
    healthcheck:
      test: ["CMD", "true"]
      interval: 5s
      timeout: 3s
      retries: 3
